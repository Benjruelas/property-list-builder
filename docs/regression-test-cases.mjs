/**
 * Manual regression test case definitions for KnockScout.
 * Generated by docs/build-regression-cases.mjs — do not edit by hand.
 * Rebuild: node docs/build-regression-cases.mjs && node docs/generate-regression-html.mjs
 */

import { tc, step, SECTIONS } from './regression-test-schema.mjs'

export { SECTIONS }

export const TEST_CASES = [
  tc("AUTH-001", "01", "Login with valid credentials", ["solo","team-admin","team-member"], "both",
    "Logged out. Test on both mobile and desktop if time allows.",
    [
      step("Open Menu → Sign In", "Sign In is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Enter email and password”) is visible and enabled.", "Auth / menu"),
      step("Enter email and password", "Every field named in “email and password” visibly retains its test value; no validation message blocks progress. The control needed for the next step (“Submit”) is visible and enabled.", "Auth / menu"),
      step("Submit", "Final-state checks: verify user lands on map; verify action bar visible.", "Auth / menu"),
    ],
    "User lands on map; action bar visible"),

  tc("AUTH-002", "01", "Login with invalid credentials", ["logged-out"], "both",
    "Logged out. Test on both mobile and desktop if time allows. User is logged out.",
    [
      step("Open Sign In", "Sign In is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Enter wrong password”) is visible and enabled.", "Auth / menu"),
      step("Enter wrong password", "Every field named in “wrong password” visibly retains its test value; no validation message blocks progress. The control needed for the next step (“Submit”) is visible and enabled.", "Auth / menu"),
      step("Submit", "Final-state checks: verify error message shown; verify still logged out.", "Auth / menu"),
    ],
    "Error message shown; still logged out"),

  tc("AUTH-003", "01", "Sign up new account", ["logged-out"], "both",
    "Logged out. Test on both mobile and desktop if time allows. User is logged out.",
    [
      step("Open Sign Up from login", "Sign Up is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Enter valid test data in every required field of the registration form”) is visible and enabled.", "Auth / menu"),
      step("Enter valid test data in every required field of the registration form", "Every field named in “valid test data in every required field of the registration form” visibly retains its test value; no validation message blocks progress. The control needed for the next step (“Submit”) is visible and enabled.", "Auth / menu"),
      step("Submit", "Final-state checks: verify account created or verification step shown.", "Auth / menu"),
    ],
    "Account created or verification step shown"),

  tc("AUTH-004", "01", "Forgot password flow", ["logged-out"], "both",
    "Logged out. Test on both mobile and desktop if time allows. User is logged out.",
    [
      step("Sign In → Forgot password", "Forgot password is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Enter email”) is visible and enabled.", "Auth / menu"),
      step("Enter email", "Every field named in “email” visibly retains its test value; no validation message blocks progress. The control needed for the next step (“Submit”) is visible and enabled.", "Auth / menu"),
      step("Submit", "Final-state checks: verify reset instructions sent message.", "Auth / menu"),
    ],
    "Reset instructions sent message"),

  tc("AUTH-005", "01", "Switch login ↔ sign up ↔ forgot", ["logged-out"], "both",
    "Logged out. Test on both mobile and desktop if time allows. User is logged out.",
    [
      step("Open Sign In modal", "Sign In modal is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Switch to Sign Up via link”) is visible and enabled.", "Auth / menu"),
      step("Switch to Sign Up via link", "The Sign Up control visibly shows the selected option or changed state, and dependent content updates to match it. The control needed for the next step (“Switch to Forgot password via link”) is visible and enabled.", "Auth / menu"),
      step("Switch to Forgot password via link", "The Forgot password control visibly shows the selected option or changed state, and dependent content updates to match it. The control needed for the next step (“Return to Sign In via link”) is visible and enabled.", "Auth / menu"),
      step("Return to Sign In via link", "Final-state checks: verify each modal opens without error.", "Auth / menu"),
    ],
    "Each modal opens without error"),

  tc("AUTH-006", "01", "Sign out", ["solo","team-admin","team-member"], "both",
    "Logged in. Test on both mobile and desktop if time allows.",
    [
      step("Menu → Settings → Logout", "Final-state checks: verify returned to logged-out state; verify sign In available.", "Auth / menu"),
    ],
    "Returned to logged-out state; Sign In available"),

  tc("AUTH-007", "01", "Gated action opens login when logged out", ["logged-out"], "both",
    "Logged out. Test on both mobile and desktop if time allows. User is logged out.",
    [
      step("Open Menu", "Menu is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Tap Leads”) is visible and enabled.", "Auth / menu"),
      step("Tap Leads", "Final-state checks: verify login modal opens instead of panel.", "Auth / menu"),
    ],
    "Login modal opens instead of panel"),

  tc("AUTH-008", "01", "Location/orientation permission prompt", ["solo","team-admin","team-member"], "both",
    "Fresh session or reset permissions. Test on both mobile and desktop if time allows.",
    [
      step("Clear the site location permission and reload the app", "The Clear the site location permission and reload the app control visibly shows the selected option or changed state, and dependent content updates to match it. The control needed for the next step (“Choose Grant and confirm the map remains usable”) is visible and enabled.", "Auth / menu"),
      step("Choose Grant and confirm the map remains usable", "The Choose Grant and confirm the map remains usable control visibly shows the selected option or changed state, and dependent content updates to match it. The control needed for the next step (“Reset the permission, reload, and choose Deny”) is visible and enabled.", "Auth / menu"),
      step("Reset the permission, reload, and choose Deny", "Final-state checks: verify permission prompt appears; verify both Grant and Deny leave the app interactive with no blank screen or uncaught-error message.", "Auth / menu"),
    ],
    "Permission prompt appears; both Grant and Deny leave the app interactive with no blank screen or uncaught-error message"),

  tc("AUTH-009", "01", "Notification permission prompt", ["solo","team-admin","team-member"], "both",
    "Logged in, notifications not granted. Test on both mobile and desktop if time allows.",
    [
      step("Clear the site notification permission and reload the app", "The Clear the site notification permission and reload the app control visibly shows the selected option or changed state, and dependent content updates to match it. The control needed for the next step (“Choose Allow or Block”) is visible and enabled.", "Auth / menu"),
      step("Choose Allow or Block", "The Choose Allow or Block control visibly shows the selected option or changed state, and dependent content updates to match it. The control needed for the next step (“Reload the app”) is visible and enabled.", "Auth / menu"),
      step("Reload the app", "Final-state checks: verify notification prompt appears; verify the selected browser permission remains set after reload.", "Auth / menu"),
    ],
    "Notification prompt appears; the selected browser permission remains set after reload"),

  tc("AUTH-010", "01", "Complete welcome tour", ["solo","team-admin","team-member"], "both",
    "New user or restart tour from Settings. Test on both mobile and desktop if time allows.",
    [
      step("Step through all tour slides", "all tour slides is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Finish tour”) is visible and enabled.", "Auth / menu"),
      step("Finish tour", "Final-state checks: verify tour dismisses; verify map usable.", "Auth / menu"),
    ],
    "Tour dismisses; map usable"),

  tc("AUTH-011", "01", "Restart tour from Settings", ["solo","team-admin","team-member"], "both",
    "Logged in. Test on both mobile and desktop if time allows.",
    [
      step("Settings → Help → Restart tour", "Final-state checks: verify welcome tour relaunches.", "Auth / menu"),
    ],
    "Welcome tour relaunches"),

  tc("AUTH-012", "01", "Feature gate denies panel for restricted member", ["team-member"], "both",
    "Member without leads feature. Test on both mobile and desktop if time allows. Signed in as: team-member.",
    [
      step("Menu → Leads", "Final-state checks: verify toast: access denied; verify panel does not open.", "Auth / menu"),
    ],
    "Toast: access denied; panel does not open"),

  tc("AUTH-013", "01", "Session persists across page reload", ["solo","team-admin","team-member"], "both",
    "Logged in. Test on both mobile and desktop if time allows.",
    [
      step("Refresh page", "The current page reloads and the application loading state or app shell becomes visible. The control needed for the next step (“Wait for app to finish loading”) is visible and enabled.", "Auth / bootstrap"),
      step("Wait for app to finish loading", "Final-state checks: verify still logged in; verify map and action bar usable without re-auth.", "Auth / bootstrap"),
    ],
    "Still logged in; map and action bar usable without re-auth"),

  tc("MAP-001", "02", "Map loads with parcels visible", ["solo","team-admin","team-member"], "both",
    "Logged in. Test on both mobile and desktop if time allows.",
    [
      step("Open app at localhost", "Final-state checks: verify satellite/street basemap renders; verify parcel outlines visible.", "Map chrome"),
    ],
    "Satellite/street basemap renders; parcel outlines visible"),

  tc("MAP-002", "02", "Address search fly-to", ["solo","team-admin","team-member"], "both",
    "Logged in. Test on both mobile and desktop if time allows.",
    [
      step("Enter address in search", "Every field named in “address in search” visibly retains its test value; no validation message blocks progress. The control needed for the next step (“Select result”) is visible and enabled.", "Map view"),
      step("Select result", "Final-state checks: verify map flies to location; verify marker/parcel context updates.", "Map view"),
    ],
    "Map flies to location; marker/parcel context updates"),

  tc("MAP-003", "02", "Zoom in/out controls", ["solo","team-admin","team-member"], "both",
    "Logged in. Test on both mobile and desktop if time allows.",
    [
      step("Use +/− zoom buttons", "Final-state checks: verify map zoom level changes.", "Map chrome"),
    ],
    "Map zoom level changes"),

  tc("MAP-004", "02", "Recenter to user location", ["solo","team-admin","team-member"], "both",
    "Location permission granted. Test on both mobile and desktop if time allows.",
    [
      step("Tap recenter/locate control", "Final-state checks: verify map centers on user position.", "Map chrome"),
    ],
    "Map centers on user position"),

  tc("MAP-005", "02", "Compass orient map", ["solo","team-admin","team-member"], "both",
    "Orientation permission granted. Test on both mobile and desktop if time allows.",
    [
      step("Tap compass control", "Final-state checks: verify map rotates to device heading.", "Map chrome"),
    ],
    "Map rotates to device heading"),

  tc("MAP-006", "02", "Parcel click opens popup", ["solo","team-admin","team-member"], "both",
    "Logged in, parcel on map. Test on both mobile and desktop if time allows.",
    [
      step("Click a parcel polygon", "Final-state checks: verify parcel popup appears at click location.", "Map chrome"),
    ],
    "Parcel popup appears at click location"),

  tc("MAP-007", "02", "Popup → Details opens Parcel Details", ["solo","team-admin","team-member"], "both",
    "Popup open. Test on both mobile and desktop if time allows.",
    [
      step("On the map, open a parcel and leave the parcel popup visible.", "Before continuing, visibly confirm these starting conditions: Popup open. The starting screen is loaded, responsive, and has no blocking error dialog.", "Map chrome"),
      step("Tap Details in popup", "Final-state checks: verify full Parcel Details overlay opens.", "Map chrome"),
    ],
    "Full Parcel Details overlay opens"),

  tc("MAP-008", "02", "Popup → Add to list", ["solo","team-admin","team-member"], "both",
    "Popup open. Test on both mobile and desktop if time allows.",
    [
      step("On the map, open a parcel and leave the parcel popup visible.", "Before continuing, visibly confirm these starting conditions: Popup open. The starting screen is loaded, responsive, and has no blocking error dialog.", "Parcel UI"),
      step("Tap Add to list", "Final-state checks: verify lists panel opens in pick-list mode with banner.", "Parcel UI"),
    ],
    "Lists panel opens in pick-list mode with banner"),

  tc("MAP-009", "02", "Popup → Convert to lead", ["solo","team-admin","team-member"], "both",
    "Popup open, parcel not a lead. Test on both mobile and desktop if time allows.",
    [
      step("On the map, open a parcel and leave the parcel popup visible.", "Before continuing, visibly confirm these starting conditions: Popup open, parcel not a lead. The starting screen is loaded, responsive, and has no blocking error dialog.", "Parcel UI"),
      step("Tap Convert to lead", "Final-state checks: verify create Lead dialog opens with parcel prefill.", "Parcel UI"),
    ],
    "Create Lead dialog opens with parcel prefill"),

  tc("MAP-010", "02", "Popup → Photos", ["solo","team-admin","team-member"], "both",
    "Popup open. Test on both mobile and desktop if time allows.",
    [
      step("On the map, open a parcel and leave the parcel popup visible.", "Before continuing, visibly confirm these starting conditions: Popup open. The starting screen is loaded, responsive, and has no blocking error dialog.", "Parcel UI"),
      step("Tap Photos", "Final-state checks: verify lead picker or photo mode flow starts.", "Parcel UI"),
    ],
    "Lead picker or photo mode flow starts"),

  tc("MAP-011", "02", "Close parcel popup", ["solo","team-admin","team-member"], "both",
    "Popup open. Test on both mobile and desktop if time allows.",
    [
      step("On the map, open a parcel and leave the parcel popup visible.", "Before continuing, visibly confirm these starting conditions: Popup open. The starting screen is loaded, responsive, and has no blocking error dialog.", "Map chrome"),
      step("Tap the popup close button", "Final-state checks: verify popup dismisses.", "Map chrome"),
    ],
    "Popup dismisses"),

  tc("MAP-012", "02", "Parcel Details — Overview tab", ["solo","team-admin","team-member"], "both",
    "Details open. Test on both mobile and desktop if time allows.",
    [
      step("Open Parcel Details for a parcel and leave the overlay visible.", "Before continuing, visibly confirm these starting conditions: Details open. The starting screen is loaded, responsive, and has no blocking error dialog.", "Map chrome"),
      step("Review Overview tab content", "Final-state checks: verify address and summary fields visible.", "Map chrome"),
    ],
    "Address and summary fields visible"),

  tc("MAP-013", "02", "Parcel Details — Contact tab", ["solo","team-admin","team-member"], "both",
    "Details open. Test on both mobile and desktop if time allows.",
    [
      step("Open Parcel Details for a parcel and leave the overlay visible.", "Before continuing, visibly confirm these starting conditions: Details open. The starting screen is loaded, responsive, and has no blocking error dialog.", "Map chrome"),
      step("Open Contact tab", "Final-state checks: verify phone/email actions available.", "Map chrome"),
    ],
    "Phone/email actions available"),

  tc("MAP-014", "02", "Parcel Details — Property tab", ["solo","team-admin","team-member"], "both",
    "Details open. Test on both mobile and desktop if time allows.",
    [
      step("Open Parcel Details for a parcel and leave the overlay visible.", "Before continuing, visibly confirm these starting conditions: Details open. The starting screen is loaded, responsive, and has no blocking error dialog.", "Map chrome"),
      step("Open Property tab", "Final-state checks: verify property fields display.", "Map chrome"),
    ],
    "Property fields display"),

  tc("MAP-015", "02", "Parcel Details — Value tab", ["solo","team-admin","team-member"], "both",
    "Details open. Test on both mobile and desktop if time allows.",
    [
      step("Open Parcel Details for a parcel and leave the overlay visible.", "Before continuing, visibly confirm these starting conditions: Details open. The starting screen is loaded, responsive, and has no blocking error dialog.", "Map chrome"),
      step("Open Value tab", "Final-state checks: verify value/assessment data shows or empty state.", "Map chrome"),
    ],
    "Value/assessment data shows or empty state"),

  tc("MAP-016", "02", "Parcel Details — Owner tab", ["solo","team-admin","team-member"], "both",
    "Details open. Test on both mobile and desktop if time allows.",
    [
      step("Open Parcel Details for a parcel and leave the overlay visible.", "Before continuing, visibly confirm these starting conditions: Details open. The starting screen is loaded, responsive, and has no blocking error dialog.", "Map chrome"),
      step("Open Owner tab", "Final-state checks: verify owner info displays.", "Map chrome"),
    ],
    "Owner info displays"),

  tc("MAP-017", "02", "Parcel Details — Legal tab", ["solo","team-admin","team-member"], "both",
    "Details open. Test on both mobile and desktop if time allows.",
    [
      step("Open Parcel Details for a parcel and leave the overlay visible.", "Before continuing, visibly confirm these starting conditions: Details open. The starting screen is loaded, responsive, and has no blocking error dialog.", "Map chrome"),
      step("Open Legal tab", "Final-state checks: verify legal fields display.", "Map chrome"),
    ],
    "Legal fields display"),

  tc("MAP-018", "02", "Edit parcel note in details", ["solo","team-admin","team-member"], "both",
    "Details open. Test on both mobile and desktop if time allows.",
    [
      step("Open Parcel Details for a parcel and leave the overlay visible.", "Before continuing, visibly confirm these starting conditions: Details open. The starting screen is loaded, responsive, and has no blocking error dialog.", "Map chrome"),
      step("Edit note field", "Every field named in “note field” visibly retains its test value; no validation message blocks progress. The control needed for the next step (“Save”) is visible and enabled.", "Map chrome"),
      step("Save", "Final-state checks: verify note persists after close/reopen.", "Map chrome"),
    ],
    "Note persists after close/reopen"),

  tc("MAP-019", "02", "Edit contacts in details", ["solo","team-admin","team-member"], "both",
    "Details open. Test on both mobile and desktop if time allows.",
    [
      step("Open Parcel Details for a parcel and leave the overlay visible.", "Before continuing, visibly confirm these starting conditions: Details open. The starting screen is loaded, responsive, and has no blocking error dialog.", "Map view"),
      step("Add a test phone number", "a test phone number appears in the current selection, list, or editor and is not marked invalid. The control needed for the next step (“Click the control to mark the phone number primary and verified”) is visible and enabled.", "Teams / Outreach"),
      step("Click the control to mark the phone number primary and verified", "control to mark the phone number primary and verified is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Save the contact changes”) is visible and enabled.", "Teams / Outreach"),
      step("Save the contact changes", "A success indicator appears, the submitted values remain visible, and no unsaved or validation state remains. The control needed for the next step (“Close and reopen Parcel Details”) is visible and enabled.", "Dialog / feedback"),
      step("Close and reopen Parcel Details", "Final-state checks: verify saved phone number is shown with primary and verified indicators after reopening.", "Map chrome"),
    ],
    "Saved phone number is shown with primary and verified indicators after reopening"),

  tc("MAP-020", "02", "Phone action from parcel contact", ["solo","team-admin","team-member"], "both",
    "Details open with phone. Test on both mobile and desktop if time allows.",
    [
      step("Open Parcel Details for a parcel and leave the overlay visible.", "Before continuing, visibly confirm these starting conditions: Details open with phone. The starting screen is loaded, responsive, and has no blocking error dialog.", "Map chrome"),
      step("Tap phone number", "Final-state checks: verify phoneActionPanel opens (text/call).", "Map chrome"),
    ],
    "PhoneActionPanel opens (text/call)"),

  tc("MAP-021", "02", "Email action from parcel contact", ["solo","team-admin","team-member"], "both",
    "Details open with email. Test on both mobile and desktop if time allows.",
    [
      step("Open Parcel Details for a parcel and leave the overlay visible.", "Before continuing, visibly confirm these starting conditions: Details open with email. The starting screen is loaded, responsive, and has no blocking error dialog.", "Map chrome"),
      step("Tap email", "Final-state checks: verify outreach/EmailComposer flow starts.", "Map chrome"),
    ],
    "Outreach/EmailComposer flow starts"),

  tc("MAP-022", "02", "Skip trace single parcel", ["solo","team-admin","team-member"], "both",
    "Details or popup. Test on both mobile and desktop if time allows.",
    [
      step("Trigger Skip trace", "Skip trace is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Wait for completion”) is visible and enabled.", "Map chrome"),
      step("Wait for completion", "Final-state checks: verify progress indicator then success/fail toast; verify badge if traced.", "Map chrome"),
    ],
    "Progress indicator then success/fail toast; badge if traced"),

  tc("MAP-023", "02", "Hail data panel from parcel", ["solo","team-admin","team-member"], "both",
    "Details open. Test on both mobile and desktop if time allows.",
    [
      step("Open Parcel Details for a parcel and leave the overlay visible.", "Before continuing, visibly confirm these starting conditions: Details open. The starting screen is loaded, responsive, and has no blocking error dialog.", "Map chrome"),
      step("Open Hail data", "Final-state checks: verify hailDataPanel lists events near parcel.", "Map chrome"),
    ],
    "HailDataPanel lists events near parcel"),

  tc("MAP-024", "02", "Hail storm overlay timeline", ["solo","team-admin","team-member"], "both",
    "Hail panel open, event selected. Test on both mobile and desktop if time allows.",
    [
      step("Open Hail data from Parcel Details.", "Before continuing, visibly confirm these starting conditions: Hail panel open, event selected. The starting screen is loaded, responsive, and has no blocking error dialog.", "Parcel UI"),
      step("Select event", "The event control visibly shows the selected option or changed state, and dependent content updates to match it. The control needed for the next step (“Scrub timeline prev/next”) is visible and enabled.", "Parcel UI"),
      step("Scrub timeline prev/next", "The timeline prev/next control visibly shows the selected option or changed state, and dependent content updates to match it. The control needed for the next step (“Exit storm”) is visible and enabled.", "Parcel UI"),
      step("Exit storm", "Final-state checks: verify radar overlay animates; verify exit returns to hail panel.", "Parcel UI"),
    ],
    "Radar overlay animates; exit returns to hail panel"),

  tc("MAP-025", "02", "Multi-select parcels and add to list", ["solo","team-admin","team-member"], "both",
    "Logged in. Test on both mobile and desktop if time allows.",
    [
      step("Enable multi-select on map", "The multi-select control visibly shows the selected option or changed state, and dependent content updates to match it. The control needed for the next step (“Tap several parcels”) is visible and enabled.", "Map chrome"),
      step("Tap several parcels", "several parcels is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Tap + Add to list”) is visible and enabled.", "Map chrome"),
      step("Tap + Add to list", "+ Add to list is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Pick list”) is visible and enabled.", "Map chrome"),
      step("Pick list", "Final-state checks: verify parcels added; verify banner shows count in Lists panel.", "Map chrome"),
    ],
    "Parcels added; banner shows count in Lists panel"),

  tc("MAP-026", "02", "Cancel multi-select mode", ["solo","team-admin","team-member"], "both",
    "Multi-select active. Test on both mobile and desktop if time allows.",
    [
      step("Enable map multi-select mode per preconditions.", "Before continuing, visibly confirm these starting conditions: Multi-select active. The starting screen is loaded, responsive, and has no blocking error dialog.", "Map chrome"),
      step("Tap cancel/X on multi-select UI", "Final-state checks: verify selection cleared; verify mode exits.", "Map chrome"),
    ],
    "Selection cleared; mode exits"),

  tc("MAP-027", "02", "Start and stop path recording", ["solo","team-admin","team-member"], "both",
    "Logged in. Test on both mobile and desktop if time allows.",
    [
      step("Start path recording from map control", "path recording is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Move/walk briefly”) is visible and enabled.", "Map chrome"),
      step("Move/walk briefly", "The Move/walk briefly control visibly shows the selected option or changed state, and dependent content updates to match it. The control needed for the next step (“Stop recording”) is visible and enabled.", "Map chrome"),
      step("Stop recording", "Final-state checks: verify path saved; verify visible in Paths panel.", "Map chrome"),
    ],
    "Path saved; visible in Paths panel"),

  tc("MAP-028", "02", "Photo Mode chooser — upload vs camera", ["solo","team-admin","team-member"], "both",
    "Photos feature enabled. Test on both mobile and desktop if time allows.",
    [
      step("Open Photo Mode for a parcel or lead", "Photo Mode for a parcel or lead is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Verify chooser shows Upload photos and Take photos buttons”) is visible and enabled.", "Map chrome"),
      step("Verify chooser shows Upload photos and Take photos buttons", "Verify chooser shows Upload photos and Take photos buttons is visible and can be checked against this case’s required state: Chooser renders both options; camera does not auto-start; back returns to chooser. The control needed for the next step (“Tap Take photos then use back to return to chooser”) is visible and enabled.", "Map view"),
      step("Tap Take photos then use back to return to chooser", "Final-state checks: verify chooser renders both options; verify camera does not auto-start; verify back returns to chooser.", "Map view"),
    ],
    "Chooser renders both options; camera does not auto-start; back returns to chooser"),

  tc("MAP-029", "02", "Photo Mode — upload photos review flow", ["solo","team-admin","team-member"], "both",
    "Photos feature enabled. Test on both mobile and desktop if time allows.",
    [
      step("Open Photo Mode", "Photo Mode is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Tap Upload photos”) is visible and enabled.", "Map view"),
      step("Tap Upload photos", "Upload photos is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Pick one or more image files”) is visible and enabled.", "Map view"),
      step("Pick one or more image files", "The one or more image files control visibly shows the selected option or changed state, and dependent content updates to match it. The control needed for the next step (“Review thumbnails in the session review screen”) is visible and enabled.", "Map view"),
      step("Review thumbnails in the session review screen", "thumbnails in the session review screen is visible and can be checked against this case’s required state: Selected photos appear in review; upload completes; photos attach to the lead. The control needed for the next step (“Confirm/attach photos”) is visible and enabled.", "Map view"),
      step("Confirm/attach photos", "Final-state checks: verify selected photos appear in review; verify upload completes; verify photos attach to the lead.", "Dialog / feedback"),
    ],
    "Selected photos appear in review; upload completes; photos attach to the lead"),

  tc("NAV-001", "03", "Open Pipes from action bar", ["solo","team-admin","team-member"], "both",
    "Logged in. Test on both mobile and desktop if time allows.",
    [
      step("Tap Pipes on action bar", "Final-state checks: verify deal pipeline panel opens.", "Action bar / menu"),
    ],
    "Deal pipeline panel opens"),

  tc("NAV-002", "03", "Open Tasks from action bar", ["solo","team-admin","team-member"], "both",
    "Logged in. Test on both mobile and desktop if time allows.",
    [
      step("Tap Tasks", "Final-state checks: verify tasks panel opens.", "Action bar / menu"),
    ],
    "Tasks panel opens"),

  tc("NAV-003", "03", "Open Schedule from action bar", ["solo","team-admin","team-member"], "both",
    "Logged in. Test on both mobile and desktop if time allows.",
    [
      step("Tap Schedule", "Final-state checks: verify schedule panel opens.", "Action bar / menu"),
    ],
    "Schedule panel opens"),

  tc("NAV-004", "03", "Open Leads from action bar", ["solo","team-admin","team-member"], "both",
    "Logged in with leads access. Test on both mobile and desktop if time allows.",
    [
      step("Tap Leads", "Final-state checks: verify leads list opens.", "Action bar / menu"),
    ],
    "Leads list opens"),

  tc("NAV-005", "03", "Open Deals from action bar", ["solo","team-admin","team-member"], "both",
    "Logged in with deals access. Test on both mobile and desktop if time allows.",
    [
      step("Tap Deals", "Final-state checks: verify deals panel opens.", "Action bar / menu"),
    ],
    "Deals panel opens"),

  tc("NAV-006", "03", "Open Quotes from action bar", ["solo","team-admin","team-member"], "desktop",
    "Wide viewport ≥1440px. Use desktop viewport (width ≥768px).",
    [
      step("Tap Quotes on bar", "Final-state checks: verify quotes panel opens.", "Action bar / menu"),
    ],
    "Quotes panel opens"),

  tc("NAV-007", "03", "Open Forms from overflow menu", ["solo","team-admin","team-member"], "mobile",
    "Narrow viewport <768px. Use mobile viewport (width <768px) or device emulation.",
    [
      step("Menu → Forms", "Final-state checks: verify forms panel opens.", "Auth / menu"),
    ],
    "Forms panel opens"),

  tc("NAV-008", "03", "Open Reports from overflow menu", ["solo","team-admin","team-member"], "mobile",
    "Forms not on bar. Use mobile viewport (width <768px) or device emulation.",
    [
      step("Menu → Reports", "Final-state checks: verify reports panel opens.", "Auth / menu"),
    ],
    "Reports panel opens"),

  tc("NAV-009", "03", "Open Lists from menu", ["solo","team-admin","team-member"], "both",
    "Logged in. Test on both mobile and desktop if time allows.",
    [
      step("Menu → Lists", "Final-state checks: verify lists panel opens.", "Auth / menu"),
    ],
    "Lists panel opens"),

  tc("NAV-010", "03", "Open Paths from menu", ["solo","team-admin","team-member"], "both",
    "Logged in. Test on both mobile and desktop if time allows.",
    [
      step("Menu → Paths", "Final-state checks: verify paths panel opens.", "Auth / menu"),
    ],
    "Paths panel opens"),

  tc("NAV-011", "03", "Open Outreach from menu", ["solo","team-admin","team-member"], "both",
    "Logged in. Test on both mobile and desktop if time allows.",
    [
      step("Menu → Outreach", "Final-state checks: verify outreach panel opens.", "Auth / menu"),
    ],
    "Outreach panel opens"),

  tc("NAV-012", "03", "Open Teams from menu", ["solo","team-admin","team-member"], "both",
    "Logged in. Test on both mobile and desktop if time allows.",
    [
      step("Menu → Teams", "Final-state checks: verify teams panel opens.", "Auth / menu"),
    ],
    "Teams panel opens"),

  tc("NAV-013", "03", "Open Activity from menu with badge", ["solo","team-admin","team-member"], "both",
    "Unread activity exists. Test on both mobile and desktop if time allows.",
    [
      step("Menu → Activity", "Final-state checks: verify activity feed opens; verify unread count visible on bar when applicable.", "Auth / menu"),
    ],
    "Activity feed opens; unread count visible on bar when applicable"),

  tc("NAV-014", "03", "Open Settings from menu", ["solo","team-admin","team-member"], "both",
    "Logged in. Test on both mobile and desktop if time allows.",
    [
      step("Menu → Settings", "Final-state checks: verify settings panel stacks on top.", "Auth / menu"),
    ],
    "Settings panel stacks on top"),

  tc("NAV-015", "03", "Panel back returns to previous view", ["solo","team-admin","team-member"], "both",
    "Nested detail open. Test on both mobile and desktop if time allows.",
    [
      step("Set up preconditions: Nested detail open. Navigate to the starting screen for this test.", "Before continuing, visibly confirm these starting conditions: Nested detail open. The starting screen is loaded, responsive, and has no blocking error dialog.", "Navigation"),
      step("Open lead detail", "lead detail is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Tap back”) is visible and enabled.", "Leads CRM"),
      step("Tap back", "Final-state checks: verify returns to leads list not map.", "Navigation"),
    ],
    "Returns to leads list not map"),

  tc("NAV-016", "03", "Tasks dock beside primary panel (desktop)", ["solo","team-admin","team-member"], "desktop",
    "Width ≥768px. Use desktop viewport (width ≥768px).",
    [
      step("Open Leads", "Leads is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Open Tasks”) is visible and enabled.", "Action bar / menu"),
      step("Open Tasks", "Final-state checks: verify leads left, Tasks right, centered group.", "Action bar / menu"),
    ],
    "Leads left, Tasks right, centered group"),

  tc("NAV-017", "03", "Close Tasks while keeping primary panel", ["solo","team-admin","team-member"], "desktop",
    "Tasks docked. Use desktop viewport (width ≥768px).",
    [
      step("Open Tasks from the action bar.", "Before continuing, visibly confirm these starting conditions: Tasks docked. The starting screen is loaded, responsive, and has no blocking error dialog.", "Action bar / menu"),
      step("Close Tasks panel", "Final-state checks: verify primary panel remains; verify tasks dismisses.", "Action bar / menu"),
    ],
    "Primary panel remains; Tasks dismisses"),

  tc("NAV-018", "03", "Solo Tasks rail when no primary panel", ["solo","team-admin","team-member"], "desktop",
    "Only Tasks open. Use desktop viewport (width ≥768px).",
    [
      step("Open Tasks alone", "Final-state checks: verify tasks on right rail.", "Action bar / menu"),
    ],
    "Tasks on right rail"),

  tc("NAV-019", "03", "Activity → lead keeps Activity+Tasks docked", ["solo","team-admin","team-member"], "desktop",
    "Activity and Tasks open. Use desktop viewport (width ≥768px).",
    [
      step("Open Menu → Activity (and Tasks if required by preconditions).", "Before continuing, visibly confirm these starting conditions: Activity and Tasks open. The starting screen is loaded, responsive, and has no blocking error dialog.", "Action bar / menu"),
      step("Open Activity item for a lead", "Final-state checks: verify lead detail overlay; verify activity+Tasks stay docked.", "Action bar / menu"),
    ],
    "Lead detail overlay; Activity+Tasks stay docked"),

  tc("NAV-020", "03", "Back from Activity-origin detail returns to Activity", ["solo","team-admin","team-member"], "both",
    "Opened lead from Activity. Test on both mobile and desktop if time allows.",
    [
      step("Set up preconditions: Opened lead from Activity. Navigate to the starting screen for this test.", "Before continuing, visibly confirm these starting conditions: Opened lead from Activity. The starting screen is loaded, responsive, and has no blocking error dialog.", "Action bar / menu"),
      step("Close/back from lead detail", "Final-state checks: verify activity feed focused again.", "Action bar / menu"),
    ],
    "Activity feed focused again"),

  tc("NAV-021", "03", "Import Photos removed from bar and menu", ["solo","team-admin","team-member"], "both",
    "Logged in. Test on both mobile and desktop if time allows.",
    [
      step("Inspect desktop action bar at ≥768px width", "desktop action bar at ≥768px width is visible and can be checked against this case’s required state: No Import Photos item anywhere; Photo Mode still present on desktop bar and as map button on mobile. The control needed for the next step (“Switch to mobile width and open the overflow Menu”) is visible and enabled.", "Auth / menu"),
      step("Switch to mobile width and open the overflow Menu", "Final-state checks: verify no Import Photos item anywhere; verify photo Mode still present on desktop bar and as map button on mobile.", "Auth / menu"),
    ],
    "No Import Photos item anywhere; Photo Mode still present on desktop bar and as map button on mobile"),

  tc("NAV-022", "03", "Action bar adapts across 768px breakpoint", ["solo","team-admin","team-member"], "both",
    "Logged in. Test on both mobile and desktop if time allows.",
    [
      step("Use responsive device tools to resize viewport from 700px to 800px and back", "The responsive viewport readout reaches each specified width and the application visibly reflows at each breakpoint. The control needed for the next step (“Verify bar items at each width”) is visible and enabled.", "Action bar / menu"),
      step("Verify bar items at each width", "Final-state checks: verify mobile shows Pipes/Tasks/Schedule + Menu; verify desktop shows full bar with no Menu; verify no duplicate or missing items after resize.", "Action bar / menu"),
    ],
    "Mobile shows Pipes/Tasks/Schedule + Menu; desktop shows full bar with no Menu; no duplicate or missing items after resize"),

  tc("NAV-023", "03", "Quick Create FAB opens create options", ["solo","team-admin","team-member"], "both",
    "Logged in. Test on both mobile and desktop if time allows.",
    [
      step("Tap the Quick Create (+) floating button", "Quick Create (+) floating button is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Review the create options shown”) is visible and enabled.", "Navigation"),
      step("Review the create options shown", "Final-state checks: verify fAB expands with Task, Lead, Deal, Quote, and Report options; verify closes on outside tap.", "Navigation"),
    ],
    "FAB expands with Task, Lead, Deal, Quote, and Report options; closes on outside tap"),

  tc("NAV-024", "03", "Quick Create — create task and lead", ["solo","team-admin"], "both",
    "Logged in. Test on both mobile and desktop if time allows.",
    [
      step("Open Quick Create (+)", "Quick Create (+) is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Create a task and save”) is visible and enabled.", "Leads CRM"),
      step("Create a task and save", "A success indicator appears, the submitted values remain visible, and no unsaved or validation state remains. The control needed for the next step (“Open Quick Create again”) is visible and enabled.", "Leads CRM"),
      step("Open Quick Create again", "Quick Create again is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Create a lead and save”) is visible and enabled.", "Leads CRM"),
      step("Create a lead and save", "Final-state checks: verify task appears in Tasks; verify lead appears in Leads; verify fAB closes cleanly after each create.", "Leads CRM"),
    ],
    "Task appears in Tasks; lead appears in Leads; FAB closes cleanly after each create"),

  tc("LST-001", "04", "Lists tabs All/Mine/Shared/On map", ["solo","team-admin","team-member"], "both",
    "Lists panel open. Test on both mobile and desktop if time allows.",
    [
      step("Open Menu → Lists and leave the Lists panel visible.", "Before continuing, visibly confirm these starting conditions: Lists panel open. The starting screen is loaded, responsive, and has no blocking error dialog.", "Map chrome"),
      step("Click All tab", "All tab is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Click Mine tab”) is visible and enabled.", "Map chrome"),
      step("Click Mine tab", "Mine tab is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Click Shared tab”) is visible and enabled.", "Map chrome"),
      step("Click Shared tab", "Shared tab is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Click On map tab”) is visible and enabled.", "Map chrome"),
      step("Click On map tab", "Final-state checks: verify only lists belonging to the selected tab remain visible and the displayed count matches them; verify counts update.", "Map chrome"),
    ],
    "List filters correctly; counts update"),

  tc("LST-002", "04", "Search lists by name", ["solo","team-admin","team-member"], "both",
    "Lists exist. Test on both mobile and desktop if time allows.",
    [
      step("Open Menu → Lists and leave the Lists panel visible.", "Before continuing, visibly confirm these starting conditions: Lists exist. The starting screen is loaded, responsive, and has no blocking error dialog.", "Lists panel"),
      step("Type in search box", "Final-state checks: verify matching lists only.", "Lists panel"),
    ],
    "Matching lists only"),

  tc("LST-003", "04", "Tag filter on lists", ["solo","team-admin","team-member"], "both",
    "Tagged lists exist. Test on both mobile and desktop if time allows.",
    [
      step("Open Menu → Lists and leave the Lists panel visible.", "Before continuing, visibly confirm these starting conditions: Tagged lists exist. The starting screen is loaded, responsive, and has no blocking error dialog.", "Lists panel"),
      step("Open filter menu", "filter menu is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Select tag”) is visible and enabled.", "Auth / menu"),
      step("Select tag", "Final-state checks: verify only tagged lists shown.", "Lists panel"),
    ],
    "Only tagged lists shown"),

  tc("LST-004", "04", "Create new list", ["solo","team-admin"], "both",
    "Logged in. Test on both mobile and desktop if time allows.",
    [
      step("Tap + create", "+ create is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Enter name”) is visible and enabled.", "Lists panel"),
      step("Enter name", "Every field named in “name” visibly retains its test value; no validation message blocks progress. The control needed for the next step (“Save”) is visible and enabled.", "Lists panel"),
      step("Save", "Final-state checks: verify list appears in All/Mine.", "Dialog / feedback"),
    ],
    "List appears in All/Mine"),

  tc("LST-005", "04", "Open list parcel view", ["solo","team-admin","team-member"], "both",
    "List with parcels. Test on both mobile and desktop if time allows.",
    [
      step("Open Menu → Lists and leave the Lists panel visible.", "Before continuing, visibly confirm these starting conditions: List with parcels. The starting screen is loaded, responsive, and has no blocking error dialog.", "Map chrome"),
      step("Click list row", "Final-state checks: verify parcelListPanel opens.", "Map chrome"),
    ],
    "ParcelListPanel opens"),

  tc("LST-006", "04", "Highlight list on map (eye open)", ["solo","team-admin","team-member"], "both",
    "List with parcels. Test on both mobile and desktop if time allows.",
    [
      step("Open Menu → Lists and leave the Lists panel visible.", "Before continuing, visibly confirm these starting conditions: List with parcels. The starting screen is loaded, responsive, and has no blocking error dialog.", "Map chrome"),
      step("Click eye icon to highlight", "Final-state checks: verify open eye; verify list colored on map; verify row highlighted.", "Map chrome"),
    ],
    "Open eye; list colored on map; row highlighted"),

  tc("LST-007", "04", "Remove map highlight (eye closed)", ["solo","team-admin","team-member"], "both",
    "List highlighted. Test on both mobile and desktop if time allows.",
    [
      step("Set up preconditions: List highlighted. Navigate to the starting screen for this test.", "Before continuing, visibly confirm these starting conditions: List highlighted. The starting screen is loaded, responsive, and has no blocking error dialog.", "Map chrome"),
      step("Click eye again", "Final-state checks: verify closed eye; verify highlight removed from map.", "Map chrome"),
    ],
    "Closed eye; highlight removed from map"),

  tc("LST-008", "04", "Max 20 highlights toast", ["solo","team-admin","team-member"], "both",
    "20 lists already highlighted. Test on both mobile and desktop if time allows.",
    [
      step("Set up preconditions: 20 lists already highlighted. Navigate to the starting screen for this test.", "Before continuing, visibly confirm these starting conditions: 20 lists already highlighted. The starting screen is loaded, responsive, and has no blocking error dialog.", "Lists panel"),
      step("Try highlight 21st", "Final-state checks: verify warning toast about maximum.", "Lists panel"),
    ],
    "Warning toast about maximum"),

  tc("LST-009", "04", "Export list CSV from menu", ["solo","team-admin"], "both",
    "Own list. Test on both mobile and desktop if time allows.",
    [
      step("Open Menu → Lists and leave the Lists panel visible.", "Before continuing, visibly confirm these starting conditions: Own list. The starting screen is loaded, responsive, and has no blocking error dialog.", "Auth / menu"),
      step("Open the row overflow menu, then select Export", "Final-state checks: verify cSV download or email flow completes.", "Auth / menu"),
    ],
    "CSV download or email flow completes"),

  tc("LST-010", "04", "Share list via email", ["solo","team-admin"], "both",
    "Own list. Test on both mobile and desktop if time allows.",
    [
      step("Open Menu → Lists and leave the Lists panel visible.", "Before continuing, visibly confirm these starting conditions: Own list. The starting screen is loaded, responsive, and has no blocking error dialog.", "Teams / Outreach"),
      step("Open the row overflow menu, then select Share", "row overflow menu, then select Share is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Add email”) is visible and enabled.", "Auth / menu"),
      step("Add email", "Every field named in “email” visibly retains its test value; no validation message blocks progress. The control needed for the next step (“Save”) is visible and enabled.", "Teams / Outreach"),
      step("Save", "Final-state checks: verify email added to share list.", "Teams / Outreach"),
    ],
    "Email added to share list"),

  tc("LST-011", "04", "Rename list inline", ["solo","team-admin"], "both",
    "Own list. Test on both mobile and desktop if time allows.",
    [
      step("Open Menu → Lists and leave the Lists panel visible.", "Before continuing, visibly confirm these starting conditions: Own list. The starting screen is loaded, responsive, and has no blocking error dialog.", "Lists panel"),
      step("Open the row overflow menu, then select Rename", "row overflow menu, then select Rename is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Edit name”) is visible and enabled.", "Auth / menu"),
      step("Edit name", "Every field named in “name” visibly retains its test value; no validation message blocks progress. The control needed for the next step (“Save”) is visible and enabled.", "Lists panel"),
      step("Save", "Final-state checks: verify name updates in list.", "Dialog / feedback"),
    ],
    "Name updates in list"),

  tc("LST-012", "04", "Edit list tags", ["solo","team-admin"], "both",
    "Own list. Test on both mobile and desktop if time allows.",
    [
      step("Open Menu → Lists and leave the Lists panel visible.", "Before continuing, visibly confirm these starting conditions: Own list. The starting screen is loaded, responsive, and has no blocking error dialog.", "Lists panel"),
      step("Open the row overflow menu, then select Tags", "row overflow menu, then select Tags is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Add/remove tags”) is visible and enabled.", "Auth / menu"),
      step("Add/remove tags", "Final-state checks: verify tags persist on list.", "Lists panel"),
    ],
    "Tags persist on list"),

  tc("LST-013", "04", "Delete list with confirm", ["solo","team-admin"], "both",
    "Own list. Test on both mobile and desktop if time allows.",
    [
      step("Open Menu → Lists and leave the Lists panel visible.", "Before continuing, visibly confirm these starting conditions: Own list. The starting screen is loaded, responsive, and has no blocking error dialog.", "Dialog / feedback"),
      step("Open the row overflow menu, then select Delete", "row overflow menu, then select Delete is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Confirm”) is visible and enabled.", "Auth / menu"),
      step("Confirm", "Final-state checks: verify list removed.", "Dialog / feedback"),
    ],
    "List removed"),

  tc("LST-014", "04", "Shared list — no owner menu actions", ["solo","team-admin","team-member"], "both",
    "Shared list not owned. Test on both mobile and desktop if time allows.",
    [
      step("Open Menu → Lists and leave the Lists panel visible.", "Before continuing, visibly confirm these starting conditions: Shared list not owned. The starting screen is loaded, responsive, and has no blocking error dialog.", "Auth / menu"),
      step("Verify ⋮ menu on shared row", "Final-state checks: verify export may show; verify rename/delete hidden per ownership.", "Auth / menu"),
    ],
    "Export may show; rename/delete hidden per ownership"),

  tc("LST-015", "04", "Add single parcel from map banner mode", ["solo","team-admin","team-member"], "both",
    "Add-to-list from popup. Test on both mobile and desktop if time allows.",
    [
      step("Set up preconditions: Add-to-list from popup. Navigate to the starting screen for this test.", "Before continuing, visibly confirm these starting conditions: Add-to-list from popup. The starting screen is loaded, responsive, and has no blocking error dialog.", "Map chrome"),
      step("Select target list in banner mode", "Final-state checks: verify parcel added; verify toast or confirmation.", "Map chrome"),
    ],
    "Parcel added; toast or confirmation"),

  tc("LST-016", "04", "Multi-select add parcels to list", ["solo","team-admin","team-member"], "both",
    "Multi-select mode active. Test on both mobile and desktop if time allows.",
    [
      step("Enable map multi-select mode per preconditions.", "Before continuing, visibly confirm these starting conditions: Multi-select mode active. The starting screen is loaded, responsive, and has no blocking error dialog.", "Map chrome"),
      step("With multi-select active, tap + Add to list on the map banner", "With multi-select active, tap + Add to list opens the next picker or dialog and preserves the current selection. The control needed for the next step (“Pick the target list”) is visible and enabled.", "Map chrome"),
      step("Pick the target list", "The target list control visibly shows the selected option or changed state, and dependent content updates to match it. The control needed for the next step (“Confirm parcels are added”) is visible and enabled.", "Map chrome"),
      step("Confirm parcels are added", "Final-state checks: verify all selected parcels in list.", "Map chrome"),
    ],
    "All selected parcels in list"),

  tc("LST-017", "04", "Parcel list expand row", ["solo","team-admin","team-member"], "both",
    "ParcelListPanel open. Test on both mobile and desktop if time allows.",
    [
      step("Open a list with parcels to reach ParcelListPanel.", "Before continuing, visibly confirm these starting conditions: ParcelListPanel open. The starting screen is loaded, responsive, and has no blocking error dialog.", "Map chrome"),
      step("Click to expand a parcel row", "Final-state checks: verify expanded details and actions visible.", "Map chrome"),
    ],
    "Expanded details and actions visible"),

  tc("LST-018", "04", "Parcel list — open details", ["solo","team-admin","team-member"], "both",
    "Expanded row. Test on both mobile and desktop if time allows.",
    [
      step("Set up preconditions: Expanded row. Navigate to the starting screen for this test.", "Before continuing, visibly confirm these starting conditions: Expanded row. The starting screen is loaded, responsive, and has no blocking error dialog.", "Map chrome"),
      step("Tap Details", "Final-state checks: verify parcel Details opens.", "Map chrome"),
    ],
    "Parcel Details opens"),

  tc("LST-019", "04", "Parcel list — skip trace from row", ["solo","team-admin","team-member"], "both",
    "Expanded row. Test on both mobile and desktop if time allows.",
    [
      step("Set up preconditions: Expanded row. Navigate to the starting screen for this test.", "Before continuing, visibly confirm these starting conditions: Expanded row. The starting screen is loaded, responsive, and has no blocking error dialog.", "Map chrome"),
      step("Tap Skip trace", "Final-state checks: verify skip trace runs.", "Map chrome"),
    ],
    "Skip trace runs"),

  tc("LST-020", "04", "Parcel list — remove parcel from list", ["solo","team-admin"], "both",
    "Own list. Test on both mobile and desktop if time allows.",
    [
      step("Open Menu → Lists and leave the Lists panel visible.", "Before continuing, visibly confirm these starting conditions: Own list. The starting screen is loaded, responsive, and has no blocking error dialog.", "Map chrome"),
      step("Click to expand a parcel row", "expand a parcel row is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Open the parcel row actions and click Remove from list”) is visible and enabled.", "Map chrome"),
      step("Open the parcel row actions and click Remove from list", "parcel row actions and click Remove is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Confirm removal”) is visible and enabled.", "Map chrome"),
      step("Confirm removal", "Final-state checks: verify parcel removed from list.", "Map chrome"),
    ],
    "Parcel removed from list"),

  tc("LED-001", "05", "Create lead from panel +", ["solo","team-admin"], "both",
    "Leads access. Test on both mobile and desktop if time allows.",
    [
      step("Open Leads from the action bar (or menu on mobile).", "Before continuing, visibly confirm these starting conditions: Leads access. The starting screen is loaded, responsive, and has no blocking error dialog.", "Leads CRM"),
      step("Tap +", "+ is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Enter valid test data in every required field”) is visible and enabled.", "Leads CRM"),
      step("Enter valid test data in every required field", "Every field named in “valid test data in every required field” visibly retains its test value; no validation message blocks progress. The control needed for the next step (“Save”) is visible and enabled.", "Leads CRM"),
      step("Save", "Final-state checks: verify lead appears in list.", "Leads CRM"),
    ],
    "Lead appears in list"),

  tc("LED-002", "05", "Search leads", ["solo","team-admin","team-member"], "both",
    "Leads exist. Test on both mobile and desktop if time allows.",
    [
      step("Open Leads from the action bar (or menu on mobile).", "Before continuing, visibly confirm these starting conditions: Leads exist. The starting screen is loaded, responsive, and has no blocking error dialog.", "Action bar / menu"),
      step("Search by name/address", "Final-state checks: verify filtered results.", "Action bar / menu"),
    ],
    "Filtered results"),

  tc("LED-003", "05", "Filter leads by status", ["solo","team-admin","team-member"], "both",
    "Leads exist. Test on both mobile and desktop if time allows.",
    [
      step("Open Leads from the action bar (or menu on mobile).", "Before continuing, visibly confirm these starting conditions: Leads exist. The starting screen is loaded, responsive, and has no blocking error dialog.", "Action bar / menu"),
      step("Change status filter", "Final-state checks: verify list filters.", "Action bar / menu"),
    ],
    "List filters"),

  tc("LED-004", "05", "Filter leads by tags", ["solo","team-admin","team-member"], "both",
    "Tagged leads. Test on both mobile and desktop if time allows.",
    [
      step("Open Leads from the action bar (or menu on mobile).", "Before continuing, visibly confirm these starting conditions: Tagged leads. The starting screen is loaded, responsive, and has no blocking error dialog.", "Action bar / menu"),
      step("Open the tag filter menu", "Final-state checks: verify filtered list.", "Auth / menu"),
    ],
    "Filtered list"),

  tc("LED-005", "05", "Sort leads recent vs follow-up", ["solo","team-admin","team-member"], "both",
    "Leads exist. Test on both mobile and desktop if time allows.",
    [
      step("Open Leads from the action bar (or menu on mobile).", "Before continuing, visibly confirm these starting conditions: Leads exist. The starting screen is loaded, responsive, and has no blocking error dialog.", "Action bar / menu"),
      step("Toggle sort", "Final-state checks: verify order changes.", "Action bar / menu"),
    ],
    "Order changes"),

  tc("LED-006", "05", "Analytics strip counts", ["solo","team-admin","team-member"], "both",
    "Leads exist. Test on both mobile and desktop if time allows.",
    [
      step("Open Leads from the action bar (or menu on mobile).", "Before continuing, visibly confirm these starting conditions: Leads exist. The starting screen is loaded, responsive, and has no blocking error dialog.", "Leads panel"),
      step("View status counts strip", "Final-state checks: verify counts match list.", "Leads panel"),
    ],
    "Counts match list"),

  tc("LED-007", "05", "Open lead detail", ["solo","team-admin","team-member"], "both",
    "Leads exist. Test on both mobile and desktop if time allows.",
    [
      step("Open Leads from the action bar (or menu on mobile).", "Before continuing, visibly confirm these starting conditions: Leads exist. The starting screen is loaded, responsive, and has no blocking error dialog.", "Leads CRM"),
      step("Click lead row", "Final-state checks: verify leadDetails opens.", "Leads CRM"),
    ],
    "LeadDetails opens"),

  tc("LED-008", "05", "Change lead status", ["solo","team-admin"], "both",
    "Lead detail open. Test on both mobile and desktop if time allows.",
    [
      step("Open a lead and leave Lead Details visible.", "Before continuing, visibly confirm these starting conditions: Lead detail open. The starting screen is loaded, responsive, and has no blocking error dialog.", "Leads CRM"),
      step("Tap status button", "status button is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Select new status”) is visible and enabled.", "Leads CRM"),
      step("Select new status", "Final-state checks: verify status updates.", "Leads CRM"),
    ],
    "Status updates"),

  tc("LED-009", "05", "Add lead note", ["solo","team-admin"], "both",
    "Lead detail open. Test on both mobile and desktop if time allows.",
    [
      step("Open a lead and leave Lead Details visible.", "Before continuing, visibly confirm these starting conditions: Lead detail open. The starting screen is loaded, responsive, and has no blocking error dialog.", "Leads CRM"),
      step("Add note in activity/notes", "Final-state checks: verify note appears in timeline.", "Action bar / menu"),
    ],
    "Note appears in timeline"),

  tc("LED-010", "05", "Lead phone/text/email actions", ["solo","team-admin","team-member"], "both",
    "Lead with phone and email contact info. Test on both mobile and desktop if time allows.",
    [
      step("Set up preconditions: Lead with phone and email contact info. Navigate to the starting screen for this test.", "Before continuing, visibly confirm these starting conditions: Lead with phone and email contact info. The starting screen is loaded, responsive, and has no blocking error dialog.", "Leads CRM"),
      step("Tap the phone number", "phone number is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Inspect PhoneActionPanel for Text and Call actions”) is visible and enabled.", "Leads CRM"),
      step("Inspect PhoneActionPanel for Text and Call actions", "PhoneActionPanel for Text and Call actions is visible and can be checked against this case’s required state: EmailComposer opens for the selected lead and the prior phone/text panels closed cleanly. The control needed for the next step (“Close PhoneActionPanel”) is visible and enabled.", "Leads CRM"),
      step("Close PhoneActionPanel", "PhoneActionPanel is no longer visible; the underlying lead phone/text/email actions context is visible and responds to input. The control needed for the next step (“Tap the text action”) is visible and enabled.", "Leads CRM"),
      step("Tap the text action", "text action is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Inspect the text composer for the selected phone number”) is visible and enabled.", "Leads CRM"),
      step("Inspect the text composer for the selected phone number", "the text composer for the selected phone number is visible and can be checked against this case’s required state: EmailComposer opens for the selected lead and the prior phone/text panels closed cleanly. The control needed for the next step (“Close the text composer”) is visible and enabled.", "Leads CRM"),
      step("Close the text composer", "the text composer is no longer visible; the underlying lead phone/text/email actions context is visible and responds to input. The control needed for the next step (“Tap the email address”) is visible and enabled.", "Leads CRM"),
      step("Tap the email address", "Final-state checks: verify emailComposer opens for the selected lead and the prior phone/text panels closed cleanly.", "Leads CRM"),
    ],
    "EmailComposer opens for the selected lead and the prior phone/text panels closed cleanly"),

  tc("LED-011", "05", "Open linked deal from lead", ["solo","team-admin","team-member"], "both",
    "Lead with deal. Test on both mobile and desktop if time allows.",
    [
      step("Set up preconditions: Lead with deal. Navigate to the starting screen for this test.", "Before continuing, visibly confirm these starting conditions: Lead with deal. The starting screen is loaded, responsive, and has no blocking error dialog.", "Leads CRM"),
      step("Tap deal in Deals section", "Final-state checks: verify deal detail opens.", "Action bar / menu"),
    ],
    "Deal detail opens"),

  tc("LED-012", "05", "Create deal from lead", ["solo","team-admin"], "both",
    "Lead detail open. Test on both mobile and desktop if time allows.",
    [
      step("Open a lead and leave Lead Details visible.", "Before continuing, visibly confirm these starting conditions: Lead detail open. The starting screen is loaded, responsive, and has no blocking error dialog.", "Leads CRM"),
      step("Create deal", "The create interface or newly created create deal from lead record is visible, with the entered values intact. The control needed for the next step (“Pick template/pipe”) is visible and enabled.", "Leads CRM"),
      step("Pick template/pipe", "The template/pipe control visibly shows the selected option or changed state, and dependent content updates to match it. The control needed for the next step (“Submit”) is visible and enabled.", "Leads CRM"),
      step("Submit", "Final-state checks: verify deal created and linked.", "Leads CRM"),
    ],
    "Deal created and linked"),

  tc("LED-013", "05", "Lead tasks — add task", ["solo","team-admin"], "both",
    "Lead detail open. Test on both mobile and desktop if time allows.",
    [
      step("Open a lead and leave Lead Details visible.", "Before continuing, visibly confirm these starting conditions: Lead detail open. The starting screen is loaded, responsive, and has no blocking error dialog.", "Action bar / menu"),
      step("Add task in Tasks section", "Final-state checks: verify task appears.", "Action bar / menu"),
    ],
    "Task appears"),

  tc("LED-014", "05", "Lead tasks — toggle complete", ["solo","team-admin"], "both",
    "Task exists. Test on both mobile and desktop if time allows.",
    [
      step("Toggle checkbox", "Final-state checks: verify task moves to completed when hidden toggle off.", "Action bar / menu"),
    ],
    "Task moves to completed when hidden toggle off"),

  tc("LED-015", "05", "Lead tasks — show completed toggle", ["solo","team-admin","team-member"], "both",
    "Completed tasks exist. Test on both mobile and desktop if time allows.",
    [
      step("Toggle show completed", "Final-state checks: verify completed tasks visible.", "Action bar / menu"),
    ],
    "Completed tasks visible"),

  tc("LED-016", "05", "Schedule at date from lead", ["solo","team-admin","team-member"], "both",
    "Lead detail open. Test on both mobile and desktop if time allows.",
    [
      step("Open a lead and leave Lead Details visible.", "Before continuing, visibly confirm these starting conditions: Lead detail open. The starting screen is loaded, responsive, and has no blocking error dialog.", "Action bar / menu"),
      step("Click View on schedule", "Final-state checks: verify schedule opens at date.", "Action bar / menu"),
    ],
    "Schedule opens at date"),

  tc("LED-017", "05", "Go to parcel on map from lead", ["solo","team-admin","team-member"], "both",
    "Lead with parcel. Test on both mobile and desktop if time allows.",
    [
      step("Set up preconditions: Lead with parcel. Navigate to the starting screen for this test.", "Before continuing, visibly confirm these starting conditions: Lead with parcel. The starting screen is loaded, responsive, and has no blocking error dialog.", "Map chrome"),
      step("Click the Go to map action", "Final-state checks: verify map centers on parcel.", "Map chrome"),
    ],
    "Map centers on parcel"),

  tc("LED-018", "05", "Edit lead via menu", ["solo","team-admin"], "both",
    "Lead detail open. Test on both mobile and desktop if time allows.",
    [
      step("Open a lead and leave Lead Details visible.", "Before continuing, visibly confirm these starting conditions: Lead detail open. The starting screen is loaded, responsive, and has no blocking error dialog.", "Auth / menu"),
      step("Open the row overflow menu, then select Edit", "Final-state checks: verify createLeadDialog in edit mode.", "Auth / menu"),
    ],
    "CreateLeadDialog in edit mode"),

  tc("LED-019", "05", "Delete lead via menu", ["solo","team-admin"], "both",
    "Lead detail open. Test on both mobile and desktop if time allows.",
    [
      step("Open a lead and leave Lead Details visible.", "Before continuing, visibly confirm these starting conditions: Lead detail open. The starting screen is loaded, responsive, and has no blocking error dialog.", "Auth / menu"),
      step("Open the row overflow menu, then select Delete", "row overflow menu, then select Delete is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Confirm”) is visible and enabled.", "Auth / menu"),
      step("Confirm", "Final-state checks: verify lead removed.", "Auth / menu"),
    ],
    "Lead removed"),

  tc("LED-020", "05", "Photo reports section", ["solo","team-admin","team-member"], "both",
    "Photos feature enabled. Test on both mobile and desktop if time allows.",
    [
      step("Create or open photo report", "Final-state checks: verify report builder or detail opens.", "Action bar / menu"),
    ],
    "Report builder or detail opens"),

  tc("LED-021", "05", "Lead photos gallery", ["solo","team-admin","team-member"], "both",
    "Photos on lead. Test on both mobile and desktop if time allows.",
    [
      step("Set up preconditions: Photos on lead. Navigate to the starting screen for this test.", "Before continuing, visibly confirm these starting conditions: Photos on lead. The starting screen is loaded, responsive, and has no blocking error dialog.", "Leads CRM"),
      step("Open photos section", "Final-state checks: verify gallery displays.", "Leads CRM"),
    ],
    "Gallery displays"),

  tc("LED-022", "05", "Convert parcel to lead creates lead", ["solo","team-admin","team-member"], "both",
    "Parcel not lead. Test on both mobile and desktop if time allows.",
    [
      step("Click a parcel polygon on the map", "a parcel polygon is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Tap Convert to lead in the parcel popup”) is visible and enabled.", "Map chrome"),
      step("Tap Convert to lead in the parcel popup", "Convert to lead in the parcel popup is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Save the new lead”) is visible and enabled.", "Map chrome"),
      step("Save the new lead", "Final-state checks: verify lead created with parcel link.", "Map chrome"),
    ],
    "Lead created with parcel link"),

  tc("LED-023", "05", "Share lead dialog opens above Lead Details", ["solo","team-admin"], "both",
    "Lead detail open. Test on both mobile and desktop if time allows.",
    [
      step("Open a lead and leave Lead Details visible.", "Before continuing, visibly confirm these starting conditions: Lead detail open. The starting screen is loaded, responsive, and has no blocking error dialog.", "Leads CRM"),
      step("Open the row overflow menu, then select Share lead", "row overflow menu, then select Share lead is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Verify dialog is on top and interactive on first click”) is visible and enabled.", "Auth / menu"),
      step("Verify dialog is on top and interactive on first click", "Verify dialog is on top and interactive on first click is visible and can be checked against this case’s required state: Share dialog renders above Lead Details; inputs respond to first click; Lead Details usable after close without extra clicks. The control needed for the next step (“Add an email or team and save”) is visible and enabled.", "Leads CRM"),
      step("Add an email or team and save", "A success indicator appears, the submitted values remain visible, and no unsaved or validation state remains. The control needed for the next step (“Close share dialog”) is visible and enabled.", "Leads CRM"),
      step("Close share dialog", "Final-state checks: verify share dialog renders above Lead Details; verify inputs respond to first click; verify lead Details usable after close without extra clicks.", "Leads CRM"),
    ],
    "Share dialog renders above Lead Details; inputs respond to first click; Lead Details usable after close without extra clicks"),

  tc("LED-024", "05", "Delete lead cascades all lead content", ["solo","team-admin"], "both",
    "Lead with photos, tasks, and notes. Test on both mobile and desktop if time allows.",
    [
      step("Set up preconditions: Lead with photos, tasks, and notes. Navigate to the starting screen for this test.", "Before continuing, visibly confirm these starting conditions: Lead with photos, tasks, and notes. The starting screen is loaded, responsive, and has no blocking error dialog.", "Leads CRM"),
      step("Open the row overflow menu, then select Delete", "row overflow menu, then select Delete is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Confirm”) is visible and enabled.", "Auth / menu"),
      step("Confirm", "The confirmation dialog closes and the affected record is visibly removed or updated in the current view. The control needed for the next step (“Search for the deleted lead”) is visible and enabled.", "Leads CRM"),
      step("Search for the deleted lead", "The search field retains the entered query and the visible results refresh for that query. The control needed for the next step (“Inspect Tasks panel and photo gallery for orphaned items”) is visible and enabled.", "Leads CRM"),
      step("Inspect Tasks panel and photo gallery for orphaned items", "Final-state checks: verify lead gone; verify its photos, tasks, and notes no longer appear anywhere.", "Action bar / menu"),
    ],
    "Lead gone; its photos, tasks, and notes no longer appear anywhere"),

  tc("LED-025", "05", "Edit lead created from parcel", ["solo","team-admin"], "both",
    "Lead converted from a parcel. Test on both mobile and desktop if time allows.",
    [
      step("Open lead detail", "lead detail is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Open the row overflow menu, then select Edit”) is visible and enabled.", "Map chrome"),
      step("Open the row overflow menu, then select Edit", "row overflow menu, then select Edit is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Change a field”) is visible and enabled.", "Auth / menu"),
      step("Change a field", "The a field control visibly shows the selected option or changed state, and dependent content updates to match it. The control needed for the next step (“Save”) is visible and enabled.", "Map chrome"),
      step("Save", "Final-state checks: verify edit dialog opens without crash; verify parcel-linked fields intact; verify changes persist.", "Map chrome"),
    ],
    "Edit dialog opens without crash; parcel-linked fields intact; changes persist"),

  tc("LED-026", "05", "Delete photo from lead gallery", ["solo","team-admin"], "both",
    "Photos on lead. Test on both mobile and desktop if time allows.",
    [
      step("Set up preconditions: Photos on lead. Navigate to the starting screen for this test.", "Before continuing, visibly confirm these starting conditions: Photos on lead. The starting screen is loaded, responsive, and has no blocking error dialog.", "Leads CRM"),
      step("Open photos section", "photos section is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Open a photo or its actions”) is visible and enabled.", "Leads CRM"),
      step("Open a photo or its actions", "a photo or its actions is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Delete photo”) is visible and enabled.", "Leads CRM"),
      step("Delete photo", "A confirmation dialog identifies the photo target and shows explicit Confirm and Cancel controls. The control needed for the next step (“Confirm”) is visible and enabled.", "Leads CRM"),
      step("Confirm", "Final-state checks: verify photo removed from gallery immediately; verify count updates; verify no broken thumbnails.", "Leads CRM"),
    ],
    "Photo removed from gallery immediately; count updates; no broken thumbnails"),

  tc("DPL-001", "06", "Deals Active tab", ["solo","team-admin","team-member"], "both",
    "Deals exist. Test on both mobile and desktop if time allows.",
    [
      step("Open Deals panel", "Final-state checks: verify active deals grouped by pipeline.", "Action bar / menu"),
    ],
    "Active deals grouped by pipeline"),

  tc("DPL-002", "06", "Deals Closed tab", ["solo","team-admin","team-member"], "both",
    "Closed deals exist. Test on both mobile and desktop if time allows.",
    [
      step("Open Deals from the action bar.", "Before continuing, visibly confirm these starting conditions: Closed deals exist. The starting screen is loaded, responsive, and has no blocking error dialog.", "Action bar / menu"),
      step("Switch to Closed", "Final-state checks: verify closed records listed.", "Action bar / menu"),
    ],
    "Closed records listed"),

  tc("DPL-003", "06", "Collapse/expand pipeline group", ["solo","team-admin","team-member"], "both",
    "Deals panel. Test on both mobile and desktop if time allows.",
    [
      step("Open Deals from the action bar.", "Before continuing, visibly confirm these starting conditions: Deals panel. The starting screen is loaded, responsive, and has no blocking error dialog.", "Deals / Pipes"),
      step("Toggle pipeline header", "Final-state checks: verify group collapses/expands.", "Deals / Pipes"),
    ],
    "Group collapses/expands"),

  tc("DPL-004", "06", "Open deal detail from Deals list", ["solo","team-admin","team-member"], "both",
    "Active deal. Test on both mobile and desktop if time allows.",
    [
      step("Set up preconditions: Active deal. Navigate to the starting screen for this test.", "Before continuing, visibly confirm these starting conditions: Active deal. The starting screen is loaded, responsive, and has no blocking error dialog.", "Action bar / menu"),
      step("Click deal row", "Final-state checks: verify dealDetails opens.", "Action bar / menu"),
    ],
    "DealDetails opens"),

  tc("DPL-005", "06", "Open closed deal record", ["solo","team-admin","team-member"], "both",
    "Closed deal. Test on both mobile and desktop if time allows.",
    [
      step("Click closed row", "Final-state checks: verify closed deal view opens.", "Deals / Pipes"),
    ],
    "Closed deal view opens"),

  tc("DPL-006", "06", "Create deal from Deals panel", ["solo","team-admin"], "both",
    "Deals access. Test on both mobile and desktop if time allows.",
    [
      step("Open Deals panel", "Deals panel is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Tap create deal (+ or header action)”) is visible and enabled.", "Action bar / menu"),
      step("Tap create deal (+ or header action)", "create deal (+ or header action) is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Enter valid values in every required deal field”) is visible and enabled.", "Action bar / menu"),
      step("Enter valid values in every required deal field", "Every field named in “valid values in every required deal field” visibly retains its test value; no validation message blocks progress. The control needed for the next step (“Click Save in the create deal dialog”) is visible and enabled.", "Action bar / menu"),
      step("Click Save in the create deal dialog", "Final-state checks: verify create deal dialog.", "Action bar / menu"),
    ],
    "Create deal dialog"),

  tc("DPL-007", "06", "Manage deal templates from Deals menu", ["solo","team-admin"], "both",
    "Deals panel. Test on both mobile and desktop if time allows.",
    [
      step("Open Deals from the action bar.", "Before continuing, visibly confirm these starting conditions: Deals panel. The starting screen is loaded, responsive, and has no blocking error dialog.", "Auth / menu"),
      step("Header ⋮ → Manage templates", "Final-state checks: verify template manager opens.", "Auth / menu"),
    ],
    "Template manager opens"),

  tc("DPL-008", "06", "Deal detail — edit title/notes", ["solo","team-admin"], "both",
    "Deal detail open. Test on both mobile and desktop if time allows.",
    [
      step("Open a deal and leave Deal Details visible.", "Before continuing, visibly confirm these starting conditions: Deal detail open. The starting screen is loaded, responsive, and has no blocking error dialog.", "Deals / Pipes"),
      step("Edit fields", "Every field named in “fields” visibly retains its test value; no validation message blocks progress. The control needed for the next step (“Save”) is visible and enabled.", "Deals / Pipes"),
      step("Save", "Final-state checks: verify changes persist.", "Deals / Pipes"),
    ],
    "Changes persist"),

  tc("DPL-009", "06", "Deal detail — upload file", ["solo","team-admin"], "both",
    "Deal detail open. Test on both mobile and desktop if time allows.",
    [
      step("Open a deal and leave Deal Details visible.", "Before continuing, visibly confirm these starting conditions: Deal detail open. The starting screen is loaded, responsive, and has no blocking error dialog.", "Deals / Pipes"),
      step("Upload file", "Final-state checks: verify file appears in files list.", "Deals / Pipes"),
    ],
    "File appears in files list"),

  tc("DPL-010", "06", "Deal detail — preview/delete file", ["solo","team-admin"], "both",
    "File attached. Test on both mobile and desktop if time allows.",
    [
      step("Set up preconditions: File attached. Navigate to the starting screen for this test.", "Before continuing, visibly confirm these starting conditions: File attached. The starting screen is loaded, responsive, and has no blocking error dialog.", "Deals / Pipes"),
      step("Click the attached file to open its preview", "attached file to open its preview is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Close the preview”) is visible and enabled.", "Deals / Pipes"),
      step("Close the preview", "the preview is no longer visible; the underlying deal detail — preview/delete file context is visible and responds to input. The control needed for the next step (“Open the file actions and choose Delete”) is visible and enabled.", "Deals / Pipes"),
      step("Open the file actions and choose Delete", "file actions and choose Delete is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Confirm deletion”) is visible and enabled.", "Deals / Pipes"),
      step("Confirm deletion", "Final-state checks: verify the selected file renders in preview; verify the deleted file no longer appears in the files list.", "Deals / Pipes"),
    ],
    "The selected file renders in preview; the deleted file no longer appears in the files list"),

  tc("DPL-011", "06", "Deal detail — create quote", ["solo","team-admin"], "both",
    "Deal detail open. Test on both mobile and desktop if time allows.",
    [
      step("Open a deal and leave Deal Details visible.", "Before continuing, visibly confirm these starting conditions: Deal detail open. The starting screen is loaded, responsive, and has no blocking error dialog.", "Deals / Pipes"),
      step("Create quote", "Final-state checks: verify quote editor opens.", "Deals / Pipes"),
    ],
    "Quote editor opens"),

  tc("DPL-012", "06", "Deal detail — open linked quote", ["solo","team-admin","team-member"], "both",
    "Quote linked. Test on both mobile and desktop if time allows.",
    [
      step("Open quote from list", "Final-state checks: verify quote detail opens.", "Deals / Pipes"),
    ],
    "Quote detail opens"),

  tc("DPL-013", "06", "Deal detail — move deal", ["solo","team-admin"], "both",
    "Deal detail open. Test on both mobile and desktop if time allows.",
    [
      step("Open a deal and leave Deal Details visible.", "Before continuing, visibly confirm these starting conditions: Deal detail open. The starting screen is loaded, responsive, and has no blocking error dialog.", "Deals / Pipes"),
      step("Open the row overflow menu, then select Move deal", "row overflow menu, then select Move deal is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Pick pipeline”) is visible and enabled.", "Auth / menu"),
      step("Pick pipeline", "Final-state checks: verify deal moves to target pipe.", "Deals / Pipes"),
    ],
    "Deal moves to target pipe"),

  tc("DPL-014", "06", "Deal detail — close deal", ["solo","team-admin"], "both",
    "Open deal. Test on both mobile and desktop if time allows.",
    [
      step("Open a deal and leave Deal Details visible.", "Before continuing, visibly confirm these starting conditions: Open deal. The starting screen is loaded, responsive, and has no blocking error dialog.", "Deals / Pipes"),
      step("Open the row overflow menu, then select Close deal", "Final-state checks: verify deal archived to closed.", "Auth / menu"),
    ],
    "Deal archived to closed"),

  tc("DPL-015", "06", "Deal detail — remove deal", ["solo","team-admin"], "both",
    "Open deal. Test on both mobile and desktop if time allows.",
    [
      step("Open a deal and leave Deal Details visible.", "Before continuing, visibly confirm these starting conditions: Open deal. The starting screen is loaded, responsive, and has no blocking error dialog.", "Deals / Pipes"),
      step("Open the row overflow menu, then select Remove", "row overflow menu, then select Remove is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Confirm”) is visible and enabled.", "Auth / menu"),
      step("Confirm", "Final-state checks: verify deal removed from pipeline.", "Deals / Pipes"),
    ],
    "Deal removed from pipeline"),

  tc("DPL-016", "06", "Deal detail — open lead overlay", ["solo","team-admin","team-member"], "both",
    "Deal with lead. Test on both mobile and desktop if time allows.",
    [
      step("View lead", "Final-state checks: verify leadDetails overlays; verify back returns to deal.", "Leads CRM"),
    ],
    "LeadDetails overlays; back returns to deal"),

  tc("DPL-017", "06", "Open Pipes kanban", ["solo","team-admin","team-member"], "both",
    "Pipeline exists. Test on both mobile and desktop if time allows.",
    [
      step("Open Deals from the action bar.", "Before continuing, visibly confirm these starting conditions: Pipeline exists. The starting screen is loaded, responsive, and has no blocking error dialog.", "Action bar / menu"),
      step("Action bar → Pipes", "Final-state checks: verify columns and deal cards visible.", "Action bar / menu"),
    ],
    "Columns and deal cards visible"),

  tc("DPL-018", "06", "Switch pipeline dropdown", ["solo","team-admin"], "both",
    "Multiple pipelines. Test on both mobile and desktop if time allows.",
    [
      step("Set up preconditions: Multiple pipelines. Navigate to the starting screen for this test.", "Before continuing, visibly confirm these starting conditions: Multiple pipelines. The starting screen is loaded, responsive, and has no blocking error dialog.", "Deals / Pipes"),
      step("Select different pipeline", "Final-state checks: verify deals/columns update.", "Deals / Pipes"),
    ],
    "Deals/columns update"),

  tc("DPL-019", "06", "Create new pipeline", ["solo","team-admin"], "both",
    "Owner/admin. Test on both mobile and desktop if time allows.",
    [
      step("Open Pipes from the action bar", "Pipes is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Open create pipeline dialog”) is visible and enabled.", "Action bar / menu"),
      step("Open create pipeline dialog", "create pipeline dialog is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Enter pipeline name and save”) is visible and enabled.", "Deals / Pipes"),
      step("Enter pipeline name and save", "Final-state checks: verify new pipeline appears.", "Deals / Pipes"),
    ],
    "New pipeline appears"),

  tc("DPL-020", "06", "Share pipeline", ["solo","team-admin"], "both",
    "Own pipeline. Test on both mobile and desktop if time allows.",
    [
      step("Share pipeline", "The sharing interface for pipeline is visible and shows the recipient or permission controls required to continue. The control needed for the next step (“Add email/team”) is visible and enabled.", "Deals / Pipes"),
      step("Add email/team", "Final-state checks: verify sharing updated.", "Deals / Pipes"),
    ],
    "Sharing updated"),

  tc("DPL-021", "06", "Edit mode — rename column", ["solo","team-admin"], "both",
    "Pipeline owner. Test on both mobile and desktop if time allows.",
    [
      step("Edit mode → rename column", "Final-state checks: verify column name updates.", "Deals / Pipes"),
    ],
    "Column name updates"),

  tc("DPL-022", "06", "Edit mode — add/delete column", ["solo","team-admin"], "both",
    "Edit mode. Test on both mobile and desktop if time allows.",
    [
      step("Add column", "column appears in the current selection, list, or editor and is not marked invalid. The control needed for the next step (“Delete empty column”) is visible and enabled.", "Deals / Pipes"),
      step("Delete empty column", "Final-state checks: verify columns update.", "Deals / Pipes"),
    ],
    "Columns update"),

  tc("DPL-023", "06", "Drag deal between columns", ["solo","team-admin"], "both",
    "Collaborate permission. Test on both mobile and desktop if time allows.",
    [
      step("Set up preconditions: Collaborate permission. Navigate to the starting screen for this test.", "Before continuing, visibly confirm these starting conditions: Collaborate permission. The starting screen is loaded, responsive, and has no blocking error dialog.", "Deals / Pipes"),
      step("Drag deal to new column", "Final-state checks: verify deal status updates; verify persists on refresh.", "Deals / Pipes"),
    ],
    "Deal status updates; persists on refresh"),

  tc("DPL-024", "06", "Move deal to next stage button", ["solo","team-admin"], "both",
    "Deal in pipe. Test on both mobile and desktop if time allows.",
    [
      step("Set up preconditions: Deal in pipe. Navigate to the starting screen for this test.", "Before continuing, visibly confirm these starting conditions: Deal in pipe. The starting screen is loaded, responsive, and has no blocking error dialog.", "Deals / Pipes"),
      step("Click → on deal card", "Final-state checks: verify deal advances one column.", "Deals / Pipes"),
    ],
    "Deal advances one column"),

  tc("DPL-025", "06", "Open deal from pipe card click", ["solo","team-admin","team-member"], "both",
    "Deal in column. Test on both mobile and desktop if time allows.",
    [
      step("Set up preconditions: Deal in column. Navigate to the starting screen for this test.", "Before continuing, visibly confirm these starting conditions: Deal in column. The starting screen is loaded, responsive, and has no blocking error dialog.", "Deals / Pipes"),
      step("Click deal card body", "Final-state checks: verify dealDetails overlay opens.", "Deals / Pipes"),
    ],
    "DealDetails overlay opens"),

  tc("DPL-026", "06", "Pipe tasks sidebar — toggle complete", ["solo","team-admin","team-member"], "both",
    "Tasks in pipe sidebar. Test on both mobile and desktop if time allows.",
    [
      step("Toggle task checkbox", "Final-state checks: verify task completion updates.", "Action bar / menu"),
    ],
    "Task completion updates"),

  tc("DPL-027", "06", "Pipe tasks — open deal from task", ["solo","team-admin","team-member"], "both",
    "Task linked to deal. Test on both mobile and desktop if time allows.",
    [
      step("Click task row", "Final-state checks: verify deal opens.", "Action bar / menu"),
    ],
    "Deal opens"),

  tc("DPL-028", "06", "Deal cards visually distinct from columns", ["solo","team-admin","team-member"], "both",
    "Deals in pipe. Test on both mobile and desktop if time allows.",
    [
      step("Compare card vs column background", "Final-state checks: verify cards readable and contrast with column wells.", "Deals / Pipes"),
    ],
    "Cards readable and contrast with column wells"),

  tc("TSK-001", "07", "Tasks panel — pipeline groups", ["solo","team-admin","team-member"], "both",
    "Pipeline tasks exist. Test on both mobile and desktop if time allows.",
    [
      step("Open Tasks", "Final-state checks: verify tasks grouped by pipeline.", "Action bar / menu"),
    ],
    "Tasks grouped by pipeline"),

  tc("TSK-002", "07", "Tasks panel — unlabeled tasks section", ["solo","team-admin","team-member"], "both",
    "Standalone tasks. Test on both mobile and desktop if time allows.",
    [
      step("Open Tasks from the action bar.", "Before continuing, visibly confirm these starting conditions: Standalone tasks. The starting screen is loaded, responsive, and has no blocking error dialog.", "Action bar / menu"),
      step("View unlabeled section", "Final-state checks: verify personal tasks listed.", "Action bar / menu"),
    ],
    "Personal tasks listed"),

  tc("TSK-003", "07", "Toggle show completed tasks", ["solo","team-admin","team-member"], "both",
    "Completed tasks exist. Test on both mobile and desktop if time allows.",
    [
      step("Eye toggle for completed", "Final-state checks: verify completed tasks show/hide.", "Action bar / menu"),
    ],
    "Completed tasks show/hide"),

  tc("TSK-004", "07", "Create new task", ["solo","team-admin"], "both",
    "Tasks panel open. Test on both mobile and desktop if time allows.",
    [
      step("Click + New task", "+ New task is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Enter valid test data in every required form field”) is visible and enabled.", "Tasks / Schedule"),
      step("Enter valid test data in every required form field", "Every field named in “valid test data in every required form field” visibly retains its test value; no validation message blocks progress. The control needed for the next step (“Save”) is visible and enabled.", "Tasks / Schedule"),
      step("Save", "Final-state checks: verify task appears in list.", "Tasks / Schedule"),
    ],
    "Task appears in list"),

  tc("TSK-005", "07", "Edit existing task", ["solo","team-admin"], "both",
    "Task exists. Test on both mobile and desktop if time allows.",
    [
      step("Open edit on task", "Final-state checks: verify edit dialog saves changes.", "Tasks / Schedule"),
    ],
    "Edit dialog saves changes"),

  tc("TSK-006", "07", "Delete task", ["solo","team-admin"], "both",
    "Task exists. Test on both mobile and desktop if time allows.",
    [
      step("Delete task", "A confirmation dialog identifies the task target and shows explicit Confirm and Cancel controls. The control needed for the next step (“Confirm”) is visible and enabled.", "Tasks / Schedule"),
      step("Confirm", "Final-state checks: verify task removed.", "Tasks / Schedule"),
    ],
    "Task removed"),

  tc("TSK-007", "07", "Open deal from task", ["solo","team-admin","team-member"], "both",
    "Task with dealId. Test on both mobile and desktop if time allows.",
    [
      step("Click the linked deal action on the task", "Final-state checks: verify deal detail opens (standalone beside Tasks if docked).", "Deals / Pipes"),
    ],
    "Deal detail opens (standalone beside Tasks if docked)"),

  tc("TSK-008", "07", "Open lead from task", ["solo","team-admin","team-member"], "both",
    "Task with leadId. Test on both mobile and desktop if time allows.",
    [
      step("Set up preconditions: Task with leadId. Navigate to the starting screen for this test.", "Before continuing, visibly confirm these starting conditions: Task with leadId. The starting screen is loaded, responsive, and has no blocking error dialog.", "Leads CRM"),
      step("Open lead from task", "Final-state checks: verify lead detail opens.", "Leads CRM"),
    ],
    "Lead detail opens"),

  tc("TSK-009", "07", "Open schedule from task", ["solo","team-admin","team-member"], "both",
    "Scheduled task. Test on both mobile and desktop if time allows.",
    [
      step("View on schedule", "Final-state checks: verify schedule opens at task time.", "Action bar / menu"),
    ],
    "Schedule opens at task time"),

  tc("TSK-010", "07", "Schedule month view", ["solo","team-admin","team-member"], "both",
    "Schedule open. Test on both mobile and desktop if time allows.",
    [
      step("Open Schedule from the action bar.", "Before continuing, visibly confirm these starting conditions: Schedule open. The starting screen is loaded, responsive, and has no blocking error dialog.", "Action bar / menu"),
      step("Select Month view", "Final-state checks: verify calendar grid renders.", "Action bar / menu"),
    ],
    "Calendar grid renders"),

  tc("TSK-011", "07", "Schedule week view", ["solo","team-admin","team-member"], "both",
    "Schedule open. Test on both mobile and desktop if time allows.",
    [
      step("Open Schedule from the action bar.", "Before continuing, visibly confirm these starting conditions: Schedule open. The starting screen is loaded, responsive, and has no blocking error dialog.", "Action bar / menu"),
      step("Select Week view", "Final-state checks: verify week grid renders.", "Action bar / menu"),
    ],
    "Week grid renders"),

  tc("TSK-012", "07", "Schedule day view", ["solo","team-admin","team-member"], "both",
    "Schedule open. Test on both mobile and desktop if time allows.",
    [
      step("Open Schedule from the action bar.", "Before continuing, visibly confirm these starting conditions: Schedule open. The starting screen is loaded, responsive, and has no blocking error dialog.", "Action bar / menu"),
      step("Select Day view", "Final-state checks: verify hour slots render.", "Action bar / menu"),
    ],
    "Hour slots render"),

  tc("TSK-013", "07", "Click schedule slot — new task", ["solo","team-admin"], "both",
    "Schedule open. Test on both mobile and desktop if time allows.",
    [
      step("Open Schedule from the action bar.", "Before continuing, visibly confirm these starting conditions: Schedule open. The starting screen is loaded, responsive, and has no blocking error dialog.", "Action bar / menu"),
      step("Click empty hour slot", "Final-state checks: verify new task dialog with time prefill.", "Action bar / menu"),
    ],
    "New task dialog with time prefill"),

  tc("TSK-014", "07", "Click existing schedule task — edit", ["solo","team-admin"], "both",
    "Task on calendar. Test on both mobile and desktop if time allows.",
    [
      step("Click task block", "Final-state checks: verify edit task dialog.", "Action bar / menu"),
    ],
    "Edit task dialog"),

  tc("TSK-015", "07", "Schedule lead overlay", ["solo","team-admin","team-member"], "both",
    "Task with lead on schedule. Test on both mobile and desktop if time allows.",
    [
      step("Set up preconditions: Task with lead on schedule. Navigate to the starting screen for this test.", "Before continuing, visibly confirm these starting conditions: Task with lead on schedule. The starting screen is loaded, responsive, and has no blocking error dialog.", "Action bar / menu"),
      step("Open lead from schedule", "Final-state checks: verify lead detail overlay; verify back to schedule.", "Action bar / menu"),
    ],
    "Lead detail overlay; back to schedule"),

  tc("TSK-016", "07", "Schedule stacked from leads — back", ["solo","team-admin","team-member"], "both",
    "Opened schedule from leads. Test on both mobile and desktop if time allows.",
    [
      step("Open Schedule from the action bar.", "Before continuing, visibly confirm these starting conditions: Opened schedule from leads. The starting screen is loaded, responsive, and has no blocking error dialog.", "Action bar / menu"),
      step("Back from schedule", "Final-state checks: verify returns to leads not map.", "Action bar / menu"),
    ],
    "Returns to leads not map"),

  tc("TSK-017", "07", "Navigate months in schedule", ["solo","team-admin","team-member"], "both",
    "Month view. Test on both mobile and desktop if time allows.",
    [
      step("Prev/next month", "Final-state checks: verify calendar updates.", "Action bar / menu"),
    ],
    "Calendar updates"),

  tc("TSK-018", "07", "Schedule today/now indicator", ["solo","team-admin","team-member"], "both",
    "Schedule on today. Test on both mobile and desktop if time allows.",
    [
      step("View current day", "Final-state checks: verify now line or highlight visible.", "Action bar / menu"),
    ],
    "Now line or highlight visible"),

  tc("PTH-001", "08", "Paths tabs All/Mine/Shared/On map", ["solo","team-admin","team-member"], "both",
    "Paths exist. Test on both mobile and desktop if time allows.",
    [
      step("Open Menu → Paths.", "Before continuing, visibly confirm these starting conditions: Paths exist. The starting screen is loaded, responsive, and has no blocking error dialog.", "Map chrome"),
      step("Click All tab", "All tab is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Click Mine tab”) is visible and enabled.", "Map chrome"),
      step("Click Mine tab", "Mine tab is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Click Shared tab”) is visible and enabled.", "Map chrome"),
      step("Click Shared tab", "Shared tab is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Click On map tab”) is visible and enabled.", "Map chrome"),
      step("Click On map tab", "Final-state checks: verify only records for the selected filter remain visible and the displayed count matches them.", "Map chrome"),
    ],
    "Filtering works"),

  tc("PTH-002", "08", "Search paths", ["solo","team-admin","team-member"], "both",
    "Paths exist. Test on both mobile and desktop if time allows.",
    [
      step("Search by name/city", "Final-state checks: verify filtered results.", "Settings / Activity / Paths"),
    ],
    "Filtered results"),

  tc("PTH-003", "08", "Toggle path visibility on map", ["solo","team-admin","team-member"], "both",
    "Path exists. Test on both mobile and desktop if time allows.",
    [
      step("Eye icon toggle", "Final-state checks: verify open/closed eye; verify path shows/hides on map.", "Map chrome"),
    ],
    "Open/closed eye; path shows/hides on map"),

  tc("PTH-004", "08", "Center path on map from row click", ["solo","team-admin","team-member"], "both",
    "Path exists. Test on both mobile and desktop if time allows.",
    [
      step("Click path row", "Final-state checks: verify map centers on path.", "Map chrome"),
    ],
    "Map centers on path"),

  tc("PTH-005", "08", "Owned path — rename via menu", ["solo","team-admin"], "both",
    "Own path. Test on both mobile and desktop if time allows.",
    [
      step("Open the row overflow menu, then select Rename", "Final-state checks: verify the edited name appears in the row and remains after closing and reopening the panel.", "Auth / menu"),
    ],
    "Inline rename works"),

  tc("PTH-006", "08", "Owned path — share", ["solo","team-admin"], "both",
    "Own path. Test on both mobile and desktop if time allows.",
    [
      step("Open the row overflow menu, then select Share", "Final-state checks: verify share dialog.", "Auth / menu"),
    ],
    "Share dialog"),

  tc("PTH-007", "08", "Owned path — delete", ["solo","team-admin"], "both",
    "Own path. Test on both mobile and desktop if time allows.",
    [
      step("Open the row overflow menu, then select Delete", "row overflow menu, then select Delete is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Confirm”) is visible and enabled.", "Auth / menu"),
      step("Confirm", "Final-state checks: verify path removed.", "Settings / Activity / Paths"),
    ],
    "Path removed"),

  tc("PTH-008", "08", "Shared path — no options button", ["solo","team-admin","team-member"], "both",
    "Shared path not owned. Test on both mobile and desktop if time allows.",
    [
      step("Inspect row actions", "Final-state checks: verify ⋮ hidden; verify eye still present.", "Settings / Activity / Paths"),
    ],
    "⋮ hidden; eye still present"),

  tc("PTH-009", "08", "Owned path — tags via menu", ["solo","team-admin"], "both",
    "Own path. Test on both mobile and desktop if time allows.",
    [
      step("Open the row overflow menu, then select Tags", "Final-state checks: verify the tag picker opens, the selected tags appear on the row, and they remain after reopening it.", "Auth / menu"),
    ],
    "Tag picker works"),

  tc("ACT-001", "08", "Activity tabs filter", ["solo","team-admin","team-member"], "both",
    "Activity items exist. Test on both mobile and desktop if time allows.",
    [
      step("Open Menu → Activity", "Activity is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Click All tab”) is visible and enabled.", "Auth / menu"),
      step("Click All tab", "All tab is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Click Leads tab”) is visible and enabled.", "Action bar / menu"),
      step("Click Leads tab", "Leads tab is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Click Deals tab”) is visible and enabled.", "Action bar / menu"),
      step("Click Deals tab", "Deals tab is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Click Tasks tab”) is visible and enabled.", "Action bar / menu"),
      step("Click Tasks tab", "Tasks tab is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Click Other tab”) is visible and enabled.", "Action bar / menu"),
      step("Click Other tab", "Final-state checks: verify only activity items belonging to the selected tab remain visible.", "Action bar / menu"),
    ],
    "Items filter correctly"),

  tc("ACT-002", "08", "Activity search", ["solo","team-admin","team-member"], "both",
    "Many items. Test on both mobile and desktop if time allows.",
    [
      step("Search feed", "Final-state checks: verify results filter.", "Action bar / menu"),
    ],
    "Results filter"),

  tc("ACT-003", "08", "Activity — open lead item", ["solo","team-admin","team-member"], "both",
    "Lead activity item. Test on both mobile and desktop if time allows.",
    [
      step("Click item", "Final-state checks: verify standalone lead detail; verify activity+Tasks docked.", "Action bar / menu"),
    ],
    "Standalone lead detail; Activity+Tasks docked"),

  tc("ACT-004", "08", "Activity — open deal item", ["solo","team-admin","team-member"], "both",
    "Deal activity item. Test on both mobile and desktop if time allows.",
    [
      step("Click item", "Final-state checks: verify standalone deal detail opens.", "Action bar / menu"),
    ],
    "Standalone deal detail opens"),

  tc("ACT-005", "08", "Activity — open list item", ["solo","team-admin","team-member"], "both",
    "List activity item. Test on both mobile and desktop if time allows.",
    [
      step("Click item", "Final-state checks: verify list parcels view; verify list highlighted on map.", "Action bar / menu"),
    ],
    "List parcels view; list highlighted on map"),

  tc("ACT-006", "08", "Activity — open path item", ["solo","team-admin","team-member"], "both",
    "Path activity item. Test on both mobile and desktop if time allows.",
    [
      step("Click item", "Final-state checks: verify map centers on path; verify activity stays.", "Action bar / menu"),
    ],
    "Map centers on path; Activity stays"),

  tc("ACT-007", "08", "Activity — open task item", ["solo","team-admin","team-member"], "both",
    "Task activity item. Test on both mobile and desktop if time allows.",
    [
      step("Click item", "Final-state checks: verify tasks panel focus.", "Action bar / menu"),
    ],
    "Tasks panel focus"),

  tc("ACT-008", "08", "Activity — back to feed after detail", ["solo","team-admin","team-member"], "both",
    "Opened item from activity. Test on both mobile and desktop if time allows.",
    [
      step("Back/close detail", "Final-state checks: verify activity feed visible.", "Action bar / menu"),
    ],
    "Activity feed visible"),

  tc("SET-001", "08", "Settings — display name", ["solo","team-admin","team-member"], "both",
    "Settings open. Test on both mobile and desktop if time allows.",
    [
      step("Open Menu → Settings.", "Before continuing, visibly confirm these starting conditions: Settings open. The starting screen is loaded, responsive, and has no blocking error dialog.", "Action bar / menu"),
      step("Edit display name", "Every field named in “display name” visibly retains its test value; no validation message blocks progress. The control needed for the next step (“Save”) is visible and enabled.", "Action bar / menu"),
      step("Save", "Final-state checks: verify name persists.", "Action bar / menu"),
    ],
    "Name persists"),

  tc("SET-002", "08", "Settings — UI theme toggle", ["solo","team-admin","team-member"], "both",
    "Settings open. Test on both mobile and desktop if time allows.",
    [
      step("Open Menu → Settings.", "Before continuing, visibly confirm these starting conditions: Settings open. The starting screen is loaded, responsive, and has no blocking error dialog.", "Action bar / menu"),
      step("Switch light/dark theme", "Final-state checks: verify theme applies app-wide.", "Action bar / menu"),
    ],
    "Theme applies app-wide"),

  tc("SET-003", "08", "Settings — map style", ["solo","team-admin","team-member"], "both",
    "Settings open. Test on both mobile and desktop if time allows.",
    [
      step("Open Menu → Settings.", "Before continuing, visibly confirm these starting conditions: Settings open. The starting screen is loaded, responsive, and has no blocking error dialog.", "Map chrome"),
      step("Change map style", "Final-state checks: verify basemap updates.", "Map chrome"),
    ],
    "Basemap updates"),

  tc("SET-004", "08", "Settings — parcel boundary color", ["solo","team-admin","team-member"], "both",
    "Settings open. Test on both mobile and desktop if time allows.",
    [
      step("Open Menu → Settings.", "Before continuing, visibly confirm these starting conditions: Settings open. The starting screen is loaded, responsive, and has no blocking error dialog.", "Map chrome"),
      step("Change boundary color", "Final-state checks: verify map parcel outlines update.", "Map chrome"),
    ],
    "Map parcel outlines update"),

  tc("SET-005", "08", "Settings — path distance units", ["solo","team-admin","team-member"], "both",
    "Settings open. Test on both mobile and desktop if time allows.",
    [
      step("Open Menu → Settings.", "Before continuing, visibly confirm these starting conditions: Settings open. The starting screen is loaded, responsive, and has no blocking error dialog.", "Action bar / menu"),
      step("Toggle miles/km", "Final-state checks: verify paths panel distances update.", "Action bar / menu"),
    ],
    "Paths panel distances update"),

  tc("SET-006", "08", "Settings — email test mode", ["solo","team-admin"], "both",
    "Settings open. Test on both mobile and desktop if time allows.",
    [
      step("Open Menu → Settings.", "Before continuing, visibly confirm these starting conditions: Settings open. The starting screen is loaded, responsive, and has no blocking error dialog.", "Action bar / menu"),
      step("Enable test mode", "Final-state checks: verify composer redirects per setting.", "Action bar / menu"),
    ],
    "Composer redirects per setting"),

  tc("SET-007", "08", "Settings — notification toggles", ["solo","team-admin","team-member"], "both",
    "Settings open. Test on both mobile and desktop if time allows.",
    [
      step("Open Menu → Settings.", "Before continuing, visibly confirm these starting conditions: Settings open. The starting screen is loaded, responsive, and has no blocking error dialog.", "Action bar / menu"),
      step("Toggle notification types", "Final-state checks: verify preferences saved.", "Action bar / menu"),
    ],
    "Preferences saved"),

  tc("SET-008", "08", "Settings — sync now", ["solo","team-admin","team-member"], "both",
    "Settings open. Test on both mobile and desktop if time allows.",
    [
      step("Open Menu → Settings.", "Before continuing, visibly confirm these starting conditions: Settings open. The starting screen is loaded, responsive, and has no blocking error dialog.", "Action bar / menu"),
      step("Tap Sync now", "Final-state checks: verify sync completes without error.", "Action bar / menu"),
    ],
    "Sync completes without error"),

  tc("SET-009", "08", "Settings — browse skip traced parcels", ["solo","team-admin","team-member"], "both",
    "Skip traced data exists. Test on both mobile and desktop if time allows.",
    [
      step("Open skip traced section", "skip traced section is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Open parcel”) is visible and enabled.", "Map chrome"),
      step("Open parcel", "Final-state checks: verify parcel details from settings path.", "Map chrome"),
    ],
    "Parcel details from settings path"),

  tc("FRM-001", "09", "Forms — upload PDF template", ["solo","team-admin"], "both",
    "Forms access. Test on both mobile and desktop if time allows.",
    [
      step("Upload PDF", "Final-state checks: verify template appears in list.", "Action bar / menu"),
    ],
    "Template appears in list"),

  tc("FRM-002", "09", "Forms — search templates", ["solo","team-admin","team-member"], "both",
    "Templates exist. Test on both mobile and desktop if time allows.",
    [
      step("Search", "Final-state checks: verify filtered list.", "Action bar / menu"),
    ],
    "Filtered list"),

  tc("FRM-003", "09", "Forms — open fill view", ["solo","team-admin","team-member"], "both",
    "Template exists. Test on both mobile and desktop if time allows.",
    [
      step("Click row", "Final-state checks: verify form fill view opens.", "Action bar / menu"),
    ],
    "Form fill view opens"),

  tc("FRM-004", "09", "Forms — preview PDF from menu", ["solo","team-admin","team-member"], "both",
    "Template exists. Test on both mobile and desktop if time allows.",
    [
      step("Open the row overflow menu, then select Preview", "Final-state checks: verify pDF preview overlay.", "Auth / menu"),
    ],
    "PDF preview overlay"),

  tc("FRM-005", "09", "Forms — edit builder", ["solo","team-admin"], "both",
    "Own template. Test on both mobile and desktop if time allows.",
    [
      step("Open the row overflow menu, then select Edit", "Final-state checks: verify form builder with field overlays.", "Auth / menu"),
    ],
    "Form builder with field overlays"),

  tc("FRM-006", "09", "Forms — share template", ["solo","team-admin"], "both",
    "Own template. Test on both mobile and desktop if time allows.",
    [
      step("Open the row overflow menu, then select Share", "Final-state checks: verify share dialog.", "Auth / menu"),
    ],
    "Share dialog"),

  tc("FRM-007", "09", "Forms — send public link", ["solo","team-admin"], "both",
    "Own template. Test on both mobile and desktop if time allows.",
    [
      step("Open the row overflow menu, then select Send link", "Final-state checks: verify link dialog with ?form= URL.", "Auth / menu"),
    ],
    "Link dialog with ?form= URL"),

  tc("FRM-008", "09", "Forms — delete template", ["solo","team-admin"], "both",
    "Own template. Test on both mobile and desktop if time allows.",
    [
      step("Open the row overflow menu, then select Delete", "row overflow menu, then select Delete is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Confirm”) is visible and enabled.", "Auth / menu"),
      step("Confirm", "Final-state checks: verify template removed.", "Action bar / menu"),
    ],
    "Template removed"),

  tc("QTE-001", "09", "Quotes — create new quote", ["solo","team-admin"], "both",
    "Quotes access. Test on both mobile and desktop if time allows.",
    [
      step("Click + Create quote", "Final-state checks: verify quote editor opens.", "Action bar / menu"),
    ],
    "Quote editor opens"),

  tc("QTE-002", "09", "Quotes — tabs Quotes/Templates/Messages", ["solo","team-admin","team-member"], "both",
    "Quotes panel. Test on both mobile and desktop if time allows.",
    [
      step("Switch tabs", "Final-state checks: verify each tab renders.", "Action bar / menu"),
    ],
    "Each tab renders"),

  tc("QTE-003", "09", "Quotes — open quote detail", ["solo","team-admin","team-member"], "both",
    "Quote exists. Test on both mobile and desktop if time allows.",
    [
      step("Open Quotes panel from the action bar.", "Before continuing, visibly confirm these starting conditions: Quote exists. The starting screen is loaded, responsive, and has no blocking error dialog.", "Action bar / menu"),
      step("Click quote row", "Final-state checks: verify quote detail view.", "Action bar / menu"),
    ],
    "Quote detail view"),

  tc("QTE-004", "09", "Quotes — edit quote", ["solo","team-admin"], "both",
    "Quote exists. Test on both mobile and desktop if time allows.",
    [
      step("Edit from menu", "Final-state checks: verify editor opens with data.", "Auth / menu"),
    ],
    "Editor opens with data"),

  tc("QTE-005", "09", "Quotes — send quote", ["solo","team-admin"], "both",
    "Quote ready. Test on both mobile and desktop if time allows.",
    [
      step("Open Quotes panel from the action bar.", "Before continuing, visibly confirm these starting conditions: Quote ready. The starting screen is loaded, responsive, and has no blocking error dialog.", "Action bar / menu"),
      step("Open quote detail for a ready quote", "quote detail for a ready quote is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Open Send quote dialog”) is visible and enabled.", "Action bar / menu"),
      step("Open Send quote dialog", "Send quote dialog is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Enter a valid recipient and complete every required send field”) is visible and enabled.", "Action bar / menu"),
      step("Enter a valid recipient and complete every required send field", "Every field named in “a valid recipient and complete every required send field” visibly retains its test value; no validation message blocks progress. The control needed for the next step (“Click Send quote”) is visible and enabled.", "Action bar / menu"),
      step("Click Send quote", "Final-state checks: verify send flow completes.", "Action bar / menu"),
    ],
    "Send flow completes"),

  tc("QTE-006", "09", "Quotes — delete quote", ["solo","team-admin"], "both",
    "Own quote. Test on both mobile and desktop if time allows.",
    [
      step("Delete", "A confirmation dialog identifies the Delete target and shows explicit Confirm and Cancel controls. The control needed for the next step (“Confirm”) is visible and enabled.", "Action bar / menu"),
      step("Confirm", "Final-state checks: verify quote removed.", "Action bar / menu"),
    ],
    "Quote removed"),

  tc("QTE-007", "09", "Quote templates CRUD", ["solo","team-admin"], "both",
    "Templates tab. Test on both mobile and desktop if time allows.",
    [
      step("Open Quotes → Templates tab", "Templates tab is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Create a new template”) is visible and enabled.", "Action bar / menu"),
      step("Create a new template", "The create interface or newly created quote templates crud record is visible, with the entered values intact. The control needed for the next step (“Edit the template and save”) is visible and enabled.", "Deals / Pipes"),
      step("Edit the template and save", "A success indicator appears, the submitted values remain visible, and no unsaved or validation state remains. The control needed for the next step (“Delete the template and confirm”) is visible and enabled.", "Deals / Pipes"),
      step("Delete the template and confirm", "Final-state checks: verify the created template appears, the edited values persist, and the deleted template disappears.", "Deals / Pipes"),
    ],
    "Templates manage correctly"),

  tc("RPT-001", "09", "Reports — create photo report", ["solo","team-admin"], "both",
    "Reports + lead with photos. Test on both mobile and desktop if time allows.",
    [
      step("Create report", "The create interface or newly created reports — create photo report record is visible, with the entered values intact. The control needed for the next step (“Pick lead/template”) is visible and enabled.", "Action bar / menu"),
      step("Pick lead/template", "Final-state checks: verify report builder opens.", "Action bar / menu"),
    ],
    "Report builder opens"),

  tc("RPT-002", "09", "Reports — open report detail", ["solo","team-admin","team-member"], "both",
    "Report exists. Test on both mobile and desktop if time allows.",
    [
      step("Open Reports panel (Menu → Reports or action bar).", "Before continuing, visibly confirm these starting conditions: Report exists. The starting screen is loaded, responsive, and has no blocking error dialog.", "Action bar / menu"),
      step("Click report", "Final-state checks: verify report detail/PDF view.", "Action bar / menu"),
    ],
    "Report detail/PDF view"),

  tc("RPT-003", "09", "Reports — edit report", ["solo","team-admin"], "both",
    "Draft report. Test on both mobile and desktop if time allows.",
    [
      step("Edit in builder", "Final-state checks: verify changes save.", "Action bar / menu"),
    ],
    "Changes save"),

  tc("RPT-004", "09", "Reports — send report", ["solo","team-admin"], "both",
    "Report ready. Test on both mobile and desktop if time allows.",
    [
      step("Open Reports panel (Menu → Reports or action bar).", "Before continuing, visibly confirm these starting conditions: Report ready. The starting screen is loaded, responsive, and has no blocking error dialog.", "Action bar / menu"),
      step("Open report detail for a ready report", "report detail for a ready report is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Open Send report dialog”) is visible and enabled.", "Action bar / menu"),
      step("Open Send report dialog", "Send report dialog is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Enter a valid recipient and complete every required send field”) is visible and enabled.", "Action bar / menu"),
      step("Enter a valid recipient and complete every required send field", "Every field named in “a valid recipient and complete every required send field” visibly retains its test value; no validation message blocks progress. The control needed for the next step (“Click Send report”) is visible and enabled.", "Action bar / menu"),
      step("Click Send report", "Final-state checks: verify the send dialog accepts its required fields and displays a success result after submission.", "Action bar / menu"),
    ],
    "Send dialog works"),

  tc("RPT-005", "09", "Reports — delete report", ["solo","team-admin"], "both",
    "Own report. Test on both mobile and desktop if time allows.",
    [
      step("Delete", "A confirmation dialog identifies the Delete target and shows explicit Confirm and Cancel controls. The control needed for the next step (“Confirm”) is visible and enabled.", "Action bar / menu"),
      step("Confirm", "Final-state checks: verify report removed.", "Action bar / menu"),
    ],
    "Report removed"),

  tc("RPT-006", "09", "Report templates tab", ["solo","team-admin","team-member"], "both",
    "Templates exist. Test on both mobile and desktop if time allows.",
    [
      step("Open Forms panel (action bar on desktop or Menu → Forms on mobile).", "Before continuing, visibly confirm these starting conditions: Templates exist. The starting screen is loaded, responsive, and has no blocking error dialog.", "Deals / Pipes"),
      step("Open Reports panel → Templates tab", "Templates tab is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Review template list”) is visible and enabled.", "Action bar / menu"),
      step("Review template list", "template list is visible and can be checked against this case’s required state: Templates list/manage. The control needed for the next step (“Open a template action from the row menu”) is visible and enabled.", "Deals / Pipes"),
      step("Open a template action from the row menu", "Final-state checks: verify templates list/manage.", "Auth / menu"),
    ],
    "Templates list/manage"),

  tc("TEM-001", "10", "Teams — accept invite", ["solo","team-admin","team-member"], "both",
    "Pending invite. Test on both mobile and desktop if time allows.",
    [
      step("Accept invite", "Final-state checks: verify member joins team.", "Teams / Outreach"),
    ],
    "Member joins team"),

  tc("TEM-002", "10", "Teams — decline invite", ["solo","team-admin","team-member"], "both",
    "Pending invite. Test on both mobile and desktop if time allows.",
    [
      step("Decline invite", "Final-state checks: verify invite removed.", "Teams / Outreach"),
    ],
    "Invite removed"),

  tc("TEM-003", "10", "Teams — create team", ["solo","team-admin"], "both",
    "Can create teams. Test on both mobile and desktop if time allows.",
    [
      step("Open Menu → Teams", "Teams is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Tap create team”) is visible and enabled.", "Auth / menu"),
      step("Tap create team", "create team is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Enter team name and save”) is visible and enabled.", "Teams / Outreach"),
      step("Enter team name and save", "Final-state checks: verify team appears in list.", "Teams / Outreach"),
    ],
    "Team appears in list"),

  tc("TEM-004", "10", "Team detail — invite member", ["solo","team-admin"], "both",
    "Team admin. Test on both mobile and desktop if time allows.",
    [
      step("Invite by email", "Final-state checks: verify invite sent/pending shown.", "Teams / Outreach"),
    ],
    "Invite sent/pending shown"),

  tc("TEM-005", "10", "Team detail — remove member", ["solo","team-admin"], "both",
    "Team admin. Test on both mobile and desktop if time allows.",
    [
      step("Remove member", "A confirmation dialog identifies the member target and shows explicit Confirm and Cancel controls. The control needed for the next step (“Confirm”) is visible and enabled.", "Teams / Outreach"),
      step("Confirm", "Final-state checks: verify member removed.", "Teams / Outreach"),
    ],
    "Member removed"),

  tc("TEM-006", "10", "Team detail — feature toggles per member", ["solo","team-admin"], "both",
    "Team admin. Test on both mobile and desktop if time allows.",
    [
      step("Toggle feature for member", "Final-state checks: verify member access changes.", "Teams / Outreach"),
    ],
    "Member access changes"),

  tc("TEM-007", "10", "Team detail — transfer ownership", ["solo","team-admin"], "both",
    "Team owner. Test on both mobile and desktop if time allows.",
    [
      step("Transfer ownership", "Final-state checks: verify new owner assigned.", "Teams / Outreach"),
    ],
    "New owner assigned"),

  tc("TEM-008", "10", "Team detail — delete team", ["solo","team-admin"], "both",
    "Team owner. Test on both mobile and desktop if time allows.",
    [
      step("Delete team", "A confirmation dialog identifies the team target and shows explicit Confirm and Cancel controls. The control needed for the next step (“Confirm”) is visible and enabled.", "Teams / Outreach"),
      step("Confirm", "Final-state checks: verify team removed.", "Teams / Outreach"),
    ],
    "Team removed"),

  tc("OUT-001", "10", "Outreach — email templates tab", ["solo","team-admin","team-member"], "both",
    "Outreach open. Test on both mobile and desktop if time allows.",
    [
      step("Open Menu → Outreach.", "Before continuing, visibly confirm these starting conditions: Outreach open. The starting screen is loaded, responsive, and has no blocking error dialog.", "Deals / Pipes"),
      step("Select the Email templates tab", "Final-state checks: verify templates listed.", "Deals / Pipes"),
    ],
    "Templates listed"),

  tc("OUT-002", "10", "Outreach — create email template", ["solo","team-admin"], "both",
    "Outreach open. Test on both mobile and desktop if time allows.",
    [
      step("Open Menu → Outreach", "Outreach is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Switch to the relevant templates tab”) is visible and enabled.", "Auth / menu"),
      step("Switch to the relevant templates tab", "The relevant templates tab control visibly shows the selected option or changed state, and dependent content updates to match it. The control needed for the next step (“Create and save a new template”) is visible and enabled.", "Deals / Pipes"),
      step("Create and save a new template", "Final-state checks: verify editor saves template.", "Deals / Pipes"),
    ],
    "Editor saves template"),

  tc("OUT-003", "10", "Outreach — text templates tab", ["solo","team-admin","team-member"], "both",
    "Outreach open. Test on both mobile and desktop if time allows.",
    [
      step("Open Menu → Outreach.", "Before continuing, visibly confirm these starting conditions: Outreach open. The starting screen is loaded, responsive, and has no blocking error dialog.", "Deals / Pipes"),
      step("Select the Text templates tab", "Final-state checks: verify sMS templates listed.", "Deals / Pipes"),
    ],
    "SMS templates listed"),

  tc("OUT-004", "10", "Outreach — use template → composer", ["solo","team-admin","team-member"], "both",
    "Template exists. Test on both mobile and desktop if time allows.",
    [
      step("Use template from parcel email flow", "Final-state checks: verify emailComposer opens with merge tags.", "Map chrome"),
    ],
    "EmailComposer opens with merge tags"),

  tc("OUT-005", "10", "PhoneActionPanel — text with template", ["solo","team-admin","team-member"], "both",
    "Parcel with phone. Test on both mobile and desktop if time allows.",
    [
      step("Phone → Text → pick template", "Final-state checks: verify template applied.", "Deals / Pipes"),
    ],
    "Template applied"),

  tc("OUT-006", "10", "PhoneActionPanel — call action", ["solo","team-admin","team-member"], "both",
    "Parcel with phone. Test on both mobile and desktop if time allows.",
    [
      step("Phone → Call", "Final-state checks: verify tel link or call UI.", "Teams / Outreach"),
    ],
    "Tel link or call UI"),

  tc("OUT-007", "10", "Bulk email — list pick → preview → send", ["solo","team-admin"], "both",
    "Template + list with emails. Test on both mobile and desktop if time allows.",
    [
      step("Open Menu → Outreach", "Outreach is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Start bulk email flow with a template selected”) is visible and enabled.", "Auth / menu"),
      step("Start bulk email flow with a template selected", "bulk email flow with a template selected is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Pick a list with recipient emails”) is visible and enabled.", "Deals / Pipes"),
      step("Pick a list with recipient emails", "The a list with recipient emails control visibly shows the selected option or changed state, and dependent content updates to match it. The control needed for the next step (“Review BulkEmailPreview and confirm send”) is visible and enabled.", "Teams / Outreach"),
      step("Review BulkEmailPreview and confirm send", "Final-state checks: verify preview then confirm send.", "Teams / Outreach"),
    ],
    "Preview then confirm send"),

  tc("XFL-001", "11", "Map popup → convert → lead detail", ["solo","team-admin","team-member"], "both",
    "Parcel not lead. Test on both mobile and desktop if time allows.",
    [
      step("Click a parcel polygon on the map", "a parcel polygon is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Tap Convert to lead in the parcel popup”) is visible and enabled.", "Map chrome"),
      step("Tap Convert to lead in the parcel popup", "Convert to lead in the parcel popup is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Complete the Create Lead form and Save”) is visible and enabled.", "Map chrome"),
      step("Complete the Create Lead form and Save", "A success indicator appears, the submitted values remain visible, and no unsaved or validation state remains. The control needed for the next step (“Open the new lead in Lead Details”) is visible and enabled.", "Map chrome"),
      step("Open the new lead in Lead Details", "Final-state checks: verify lead exists and opens in detail.", "Map chrome"),
    ],
    "Lead exists and opens in detail"),

  tc("XFL-002", "11", "Map → add to list → parcel list", ["solo","team-admin","team-member"], "both",
    "Parcel on map. Test on both mobile and desktop if time allows.",
    [
      step("Click a parcel polygon on the map", "a parcel polygon is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Tap Add to list in the parcel popup”) is visible and enabled.", "Map chrome"),
      step("Tap Add to list in the parcel popup", "Add to list in the parcel popup is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Select the target list in banner mode”) is visible and enabled.", "Map chrome"),
      step("Select the target list in banner mode", "The target list in banner mode control visibly shows the selected option or changed state, and dependent content updates to match it. The control needed for the next step (“Open the list parcel view to confirm the parcel”) is visible and enabled.", "Map chrome"),
      step("Open the list parcel view to confirm the parcel", "Final-state checks: verify parcel in list; verify the parcel appears as an expandable row in the parcel list view.", "Map chrome"),
    ],
    "Parcel in list; parcel list view works"),

  tc("XFL-003", "11", "Multi-select → list → verify on map", ["solo","team-admin","team-member"], "both",
    "Multiple parcels. Test on both mobile and desktop if time allows.",
    [
      step("Add all to list", "all to list appears in the current selection, list, or editor and is not marked invalid. The control needed for the next step (“Highlight list”) is visible and enabled.", "Map chrome"),
      step("Highlight list", "Final-state checks: verify all parcels on map highlight.", "Map chrome"),
    ],
    "All parcels on map highlight"),

  tc("XFL-004", "11", "Lead → create deal → visible in Pipes", ["solo","team-admin"], "both",
    "Lead without deal. Test on both mobile and desktop if time allows.",
    [
      step("Create deal from lead", "Final-state checks: verify deal card in correct pipeline column.", "Action bar / menu"),
    ],
    "Deal card in correct pipeline column"),

  tc("XFL-005", "11", "Lead detail → open deal → back to lead", ["solo","team-admin","team-member"], "both",
    "Linked deal. Test on both mobile and desktop if time allows.",
    [
      step("Open deal", "deal is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Back”) is visible and enabled.", "Leads CRM"),
      step("Back", "Final-state checks: verify returns to lead detail.", "Leads CRM"),
    ],
    "Returns to lead detail"),

  tc("XFL-006", "11", "Deal detail → lead overlay → back to deal", ["solo","team-admin","team-member"], "both",
    "Deal with lead. Test on both mobile and desktop if time allows.",
    [
      step("Open lead overlay", "lead overlay is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Back”) is visible and enabled.", "Leads CRM"),
      step("Back", "Final-state checks: verify deal detail restored.", "Leads CRM"),
    ],
    "Deal detail restored"),

  tc("XFL-007", "11", "Deal → create quote → return to deal", ["solo","team-admin"], "both",
    "Open deal. Test on both mobile and desktop if time allows.",
    [
      step("Open a deal and leave Deal Details visible.", "Before continuing, visibly confirm these starting conditions: Open deal. The starting screen is loaded, responsive, and has no blocking error dialog.", "Deals / Pipes"),
      step("Create quote", "The create interface or newly created deal → create quote → return to deal record is visible, with the entered values intact. The control needed for the next step (“Save”) is visible and enabled.", "Deals / Pipes"),
      step("Save", "A success indicator appears, the submitted values remain visible, and no unsaved or validation state remains. The control needed for the next step (“Back from quote”) is visible and enabled.", "Deals / Pipes"),
      step("Back from quote", "Final-state checks: verify deal still visible under stack.", "Deals / Pipes"),
    ],
    "Deal still visible under stack"),

  tc("XFL-008", "11", "Task → deal detail beside docked Tasks", ["solo","team-admin","team-member"], "desktop",
    "Tasks docked. Use desktop viewport (width ≥768px).",
    [
      step("Open Tasks from the action bar.", "Before continuing, visibly confirm these starting conditions: Tasks docked. The starting screen is loaded, responsive, and has no blocking error dialog.", "Action bar / menu"),
      step("Open deal from task", "Final-state checks: verify deal detail; verify tasks remain on rail.", "Action bar / menu"),
    ],
    "Deal detail; Tasks remain on rail"),

  tc("XFL-009", "11", "Task → lead detail beside docked Tasks", ["solo","team-admin","team-member"], "desktop",
    "Tasks docked. Use desktop viewport (width ≥768px).",
    [
      step("Open Tasks from the action bar.", "Before continuing, visibly confirm these starting conditions: Tasks docked. The starting screen is loaded, responsive, and has no blocking error dialog.", "Action bar / menu"),
      step("Open lead from task", "Final-state checks: verify lead detail; verify tasks on rail.", "Action bar / menu"),
    ],
    "Lead detail; Tasks on rail"),

  tc("XFL-010", "11", "Activity → lead → back to Activity+Tasks", ["solo","team-admin","team-member"], "desktop",
    "Activity+Tasks open. Use desktop viewport (width ≥768px).",
    [
      step("Open Menu → Activity (and Tasks if required by preconditions).", "Before continuing, visibly confirm these starting conditions: Activity+Tasks open. The starting screen is loaded, responsive, and has no blocking error dialog.", "Action bar / menu"),
      step("Open lead activity", "lead activity is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Back”) is visible and enabled.", "Action bar / menu"),
      step("Back", "Final-state checks: verify activity feed focused; verify dock intact.", "Action bar / menu"),
    ],
    "Activity feed focused; dock intact"),

  tc("XFL-011", "11", "Activity → list → back to Activity", ["solo","team-admin","team-member"], "both",
    "Activity open. Test on both mobile and desktop if time allows.",
    [
      step("Open list activity", "list activity is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Back from parcels”) is visible and enabled.", "Action bar / menu"),
      step("Back from parcels", "Final-state checks: verify activity feed.", "Map chrome"),
    ],
    "Activity feed"),

  tc("XFL-012", "11", "Activity → deal → back to Activity", ["solo","team-admin","team-member"], "both",
    "Activity open. Test on both mobile and desktop if time allows.",
    [
      step("Open deal activity", "deal activity is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Back”) is visible and enabled.", "Action bar / menu"),
      step("Back", "Final-state checks: verify activity feed.", "Action bar / menu"),
    ],
    "Activity feed"),

  tc("XFL-013", "11", "Pipes drag-drop persists after reload", ["solo","team-admin"], "both",
    "Deal in pipe. Test on both mobile and desktop if time allows.",
    [
      step("Move deal column", "The deal column control visibly shows the selected option or changed state, and dependent content updates to match it. The control needed for the next step (“Refresh page”) is visible and enabled.", "Action bar / menu"),
      step("Refresh page", "Final-state checks: verify deal stays in new column.", "Action bar / menu"),
    ],
    "Deal stays in new column"),

  tc("XFL-014", "11", "Share list with team visibility", ["solo","team-admin"], "both",
    "Team member account. Test on both mobile and desktop if time allows.",
    [
      step("Share list to team", "The sharing interface for list to team is visible and shows the recipient or permission controls required to continue. The control needed for the next step (“Sign out, then sign in with the team-member test account”) is visible and enabled.", "Teams / Outreach"),
      step("Sign out, then sign in with the team-member test account", "Final-state checks: verify member sees list in Shared tab.", "Auth / menu"),
    ],
    "Member sees list in Shared tab"),

  tc("XFL-015", "11", "Share path with team visibility", ["solo","team-admin"], "both",
    "Team member account. Test on both mobile and desktop if time allows.",
    [
      step("Share path to team", "The sharing interface for path to team is visible and shows the recipient or permission controls required to continue. The control needed for the next step (“Sign out, then sign in with the team-member test account”) is visible and enabled.", "Settings / Activity / Paths"),
      step("Sign out, then sign in with the team-member test account", "Final-state checks: verify path in Shared tab; verify no ⋮ menu.", "Auth / menu"),
    ],
    "Path in Shared tab; no ⋮ menu"),

  tc("XFL-016", "11", "Share pipeline with collaborator", ["solo","team-admin"], "both",
    "Second user email. Test on both mobile and desktop if time allows.",
    [
      step("Share pipe", "The sharing interface for pipe is visible and shows the recipient or permission controls required to continue. The control needed for the next step (“Sign in as the invited user, then open Pipes”) is visible and enabled.", "Deals / Pipes"),
      step("Sign in as the invited user, then open Pipes", "Final-state checks: verify collaborator sees deals; verify can drag if permitted.", "Auth / menu"),
    ],
    "Collaborator sees deals; can drag if permitted"),

  tc("XFL-017", "11", "Schedule from deal at specific date", ["solo","team-admin","team-member"], "both",
    "Deal detail open. Test on both mobile and desktop if time allows.",
    [
      step("Open a deal and leave Deal Details visible.", "Before continuing, visibly confirm these starting conditions: Deal detail open. The starting screen is loaded, responsive, and has no blocking error dialog.", "Action bar / menu"),
      step("Click the Schedule action and choose a date", "Final-state checks: verify schedule opens on chosen date.", "Action bar / menu"),
    ],
    "Schedule opens on chosen date"),

  tc("XFL-018", "11", "Email from parcel → outreach → composer", ["solo","team-admin","team-member"], "both",
    "Parcel with email. Test on both mobile and desktop if time allows.",
    [
      step("Open Parcel Details Contact tab for a parcel with email", "Parcel Details Contact tab for a parcel with email is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Tap the email address”) is visible and enabled.", "Map chrome"),
      step("Tap the email address", "email address is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Pick an outreach template”) is visible and enabled.", "Map chrome"),
      step("Pick an outreach template", "The an outreach template control visibly shows the selected option or changed state, and dependent content updates to match it. The control needed for the next step (“Review EmailComposer content before sending or closing”) is visible and enabled.", "Map chrome"),
      step("Review EmailComposer content before sending or closing", "Final-state checks: verify composer opens with template content.", "Map chrome"),
    ],
    "Composer opens with template content"),

  tc("XFL-019", "11", "Photo mode — capture and attach to lead", ["solo","team-admin","team-member"], "both",
    "Lead exists. Test on both mobile and desktop if time allows.",
    [
      step("Open the parcel photo actions and select Photo Mode", "parcel photo actions and select Photo Mode is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Capture a photo or upload an image file”) is visible and enabled.", "Map chrome"),
      step("Capture a photo or upload an image file", "The values or media produced by “Capture a photo or upload an image file” appear in the current review state with no validation error. The control needed for the next step (“Tap Done”) is visible and enabled.", "Leads CRM"),
      step("Tap Done", "Final-state checks: verify photos on lead gallery.", "Leads CRM"),
    ],
    "Photos on lead gallery"),

  tc("XFL-020", "11", "Settings stacks over Leads panel", ["solo","team-admin","team-member"], "both",
    "Leads open. Test on both mobile and desktop if time allows.",
    [
      step("Open Settings", "Settings is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Close Settings”) is visible and enabled.", "Action bar / menu"),
      step("Close Settings", "Final-state checks: verify leads still open underneath.", "Action bar / menu"),
    ],
    "Leads still open underneath"),

  tc("XFL-021", "11", "Leads → Schedule stacked → back to Leads", ["solo","team-admin","team-member"], "both",
    "Leads open. Test on both mobile and desktop if time allows.",
    [
      step("Open schedule from lead context", "schedule is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Back”) is visible and enabled.", "Action bar / menu"),
      step("Back", "Final-state checks: verify leads panel returns.", "Action bar / menu"),
    ],
    "Leads panel returns"),

  tc("XFL-022", "11", "Deals list → detail → move deal dialog", ["solo","team-admin"], "both",
    "Deal open. Test on both mobile and desktop if time allows.",
    [
      step("Move deal between pipes", "Final-state checks: verify deal appears in target pipeline.", "Action bar / menu"),
    ],
    "Deal appears in target pipeline"),

  tc("XFL-023", "11", "Closed deal from Deals → closed view", ["solo","team-admin","team-member"], "both",
    "Closed deal exists. Test on both mobile and desktop if time allows.",
    [
      step("Open from Closed tab", "Final-state checks: verify read-only closed record.", "Action bar / menu"),
    ],
    "Read-only closed record"),

  tc("XFL-024", "11", "List bulk email pick mode", ["solo","team-admin"], "both",
    "Outreach template selected. Test on both mobile and desktop if time allows.",
    [
      step("Enter bulk mode", "Every field named in “bulk mode” visibly retains its test value; no validation message blocks progress. The control needed for the next step (“Pick list”) is visible and enabled.", "Teams / Outreach"),
      step("Pick list", "Final-state checks: verify bulkEmailPreview opens.", "Teams / Outreach"),
    ],
    "BulkEmailPreview opens"),

  tc("XFL-025", "11", "Lead from Activity does not open Leads list panel", ["solo","team-admin","team-member"], "both",
    "Activity+Tasks. Test on both mobile and desktop if time allows.",
    [
      step("Open Menu → Activity (and Tasks if required by preconditions).", "Before continuing, visibly confirm these starting conditions: Activity+Tasks. The starting screen is loaded, responsive, and has no blocking error dialog.", "Action bar / menu"),
      step("Open lead from feed", "Final-state checks: verify no Leads list underneath detail.", "Action bar / menu"),
    ],
    "No Leads list underneath detail"),

  tc("PUB-001", "12", "Public form page loads and submits", ["solo","team-admin","team-member"], "both",
    "Valid ?form= link. Test on both mobile and desktop if time allows.",
    [
      step("Open public URL", "public URL is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Enter valid test data in every required field”) is visible and enabled.", "Forms / Quotes / Reports"),
      step("Enter valid test data in every required field", "Every field named in “valid test data in every required field” visibly retains its test value; no validation message blocks progress. The control needed for the next step (“Submit”) is visible and enabled.", "Forms / Quotes / Reports"),
      step("Submit", "Final-state checks: verify success/thank you state.", "Forms / Quotes / Reports"),
    ],
    "Success/thank you state"),

  tc("PUB-002", "12", "Public quote page loads", ["solo","team-admin","team-member"], "both",
    "Valid ?quote= link. Test on both mobile and desktop if time allows.",
    [
      step("Open public URL", "Final-state checks: verify quote displays read-only.", "Forms / Quotes / Reports"),
    ],
    "Quote displays read-only"),

  tc("PUB-003", "12", "Public report page loads", ["solo","team-admin","team-member"], "both",
    "Valid ?report= link. Test on both mobile and desktop if time allows.",
    [
      step("Open public URL", "Final-state checks: verify report displays.", "Forms / Quotes / Reports"),
    ],
    "Report displays"),

  tc("PUB-004", "12", "Destructive delete shows confirm dialog", ["solo","team-admin"], "both",
    "Deletable resource. Test on both mobile and desktop if time allows.",
    [
      step("Trigger delete", "Final-state checks: verify confirm dialog blocks until confirmed.", "Dialog / feedback"),
    ],
    "Confirm dialog blocks until confirmed"),

  tc("PUB-005", "12", "Toast appears on success action", ["solo","team-admin","team-member"], "both",
    "Any save action. Test on both mobile and desktop if time allows.",
    [
      step("Perform save", "Final-state checks: verify green/success toast visible.", "Forms / Quotes / Reports"),
    ],
    "Green/success toast visible"),

  tc("PUB-006", "12", "Basemap error banner if tiles fail", ["solo","team-admin","team-member"], "both",
    "Simulate offline or block tiles. Test on both mobile and desktop if time allows.",
    [
      step("Load map", "Final-state checks: verify error banner or graceful degradation.", "Map chrome"),
    ],
    "Error banner or graceful degradation"),

  tc("PUB-007", "12", "Menu Sign In when logged out", ["logged-out"], "both",
    "Logged out. Test on both mobile and desktop if time allows. User is logged out.",
    [
      step("Menu shows Sign In", "Final-state checks: verify tapping opens login.", "Auth / menu"),
    ],
    "Tapping opens login"),

  tc("PUB-008", "12", "Restricted member deal amounts hidden", ["team-member"], "both",
    "Member without dealAmounts. Test on both mobile and desktop if time allows. Signed in as: team-member.",
    [
      step("Open deal detail", "Final-state checks: verify amount/profit fields hidden or masked.", "Deals / Pipes"),
    ],
    "Amount/profit fields hidden or masked"),

  tc("PUB-009", "12", "Public short quote link /q/ loads", ["solo","team-admin","team-member"], "both",
    "Valid /q/<token> link. Test on both mobile and desktop if time allows.",
    [
      step("Open public URL in a fresh/incognito tab", "Final-state checks: verify quote renders read-only without requiring login.", "Forms / Quotes / Reports"),
    ],
    "Quote renders read-only without requiring login"),

  tc("PUB-010", "12", "Public short report link /r/ loads", ["solo","team-admin","team-member"], "both",
    "Valid /r/<token> link. Test on both mobile and desktop if time allows.",
    [
      step("Open public URL in a fresh/incognito tab", "Final-state checks: verify report renders without requiring login.", "Forms / Quotes / Reports"),
    ],
    "Report renders without requiring login"),

  tc("PUB-011", "12", "Sync failure while offline shows feedback", ["solo","team-admin","team-member"], "both",
    "Logged in with unsaved change. Test on both mobile and desktop if time allows.",
    [
      step("Set devtools Network throttling to Offline", "The current UI visibly reflects “Set devtools Network throttling to Offline” and preserves the data needed by the next step; no error state is present. The control needed for the next step (“Open Settings and click Sync now”) is visible and enabled.", "Settings / Activity / Paths"),
      step("Open Settings and click Sync now", "Settings and click Sync now is visible and interactive, with no error banner, blank content area, or disabled required control. The control needed for the next step (“Record the offline error while confirming the unsaved change remains visible”) is visible and enabled.", "Action bar / menu"),
      step("Record the offline error while confirming the unsaved change remains visible", "The current UI visibly reflects “Record the offline error while confirming the unsaved change remains visible” and preserves the data needed by the next step; no error state is present. The control needed for the next step (“Restore the network connection”) is visible and enabled.", "Settings / Activity / Paths"),
      step("Restore the network connection", "The current UI visibly reflects “Restore the network connection” and preserves the data needed by the next step; no error state is present. The control needed for the next step (“Click Sync now again”) is visible and enabled.", "Settings / Activity / Paths"),
      step("Click Sync now again", "Final-state checks: verify clear error feedback appears while offline; verify the unsaved change remains intact; verify the second sync succeeds once online.", "Settings / Activity / Paths"),
    ],
    "Clear error feedback appears while offline; the unsaved change remains intact; the second sync succeeds once online"),

  tc("PUB-012", "12", "Invalid public token shows friendly error", ["solo","team-admin","team-member"], "both",
    "Malformed or expired ?quote=/?report=/?form= link. Test on both mobile and desktop if time allows.",
    [
      step("Open the invalid public URL", "Final-state checks: verify friendly not-found/expired message; verify no blank screen or console crash.", "Forms / Quotes / Reports"),
    ],
    "Friendly not-found/expired message; no blank screen or console crash"),
]
